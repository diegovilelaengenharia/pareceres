---
phase: 5-templates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [_Sistema_Interno/01_Motor_Python/template_checker.py]
autonomous: true
requirements: ["FR-03", "NFR-01"]
must_haves:
  truths:
    - "O utilitário template_checker.py identifica campos requisitados pelo motor em geradores/__init__.py e componentes.py."
    - "O utilitário mapeia chaves presentes nos arquivos JSON em 0_Modelos_Prontos/ e templates/."
    - "O relatório final destaca campos órfãos e campos ausentes."
    - "A consistência dos aliases em _aliases.py é verificada."
  artifacts:
    - path: "_Sistema_Interno/01_Motor_Python/template_checker.py"
      provides: "Utilitário de auditoria de consistência de dados e templates"
  key_links:
    - from: "_Sistema_Interno/01_Motor_Python/template_checker.py"
      to: "_Sistema_Interno/01_Motor_Python/geradores/_aliases.py"
      via: "importação direta para resolução de nomes"
---

<objective>
Criar um utilitário de auditoria para detectar dessincronização entre os dados fornecidos (JSON) e os dados consumidos pelo código (Python).

Purpose: Garantir que mudanças no código ou nos modelos não quebrem a geração de documentos por falta de campos ou uso de campos obsoletos.
Output: Script `template_checker.py` funcional com relatório detalhado.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.gemini/plans/PROJECT.md
@.gemini/plans/ROADMAP.md
@.gemini/plans/STATE.md
@_Sistema_Interno/01_Motor_Python/geradores/__init__.py
@_Sistema_Interno/01_Motor_Python/componentes.py
@_Sistema_Interno/01_Motor_Python/geradores/_aliases.py
@_Sistema_Interno/01_Motor_Python/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar extrator de campos do código-fonte</name>
  <files>_Sistema_Interno/01_Motor_Python/template_checker.py</files>
  <action>
    Criar a estrutura base do `template_checker.py`.
    Implementar lógica para extrair via Regex todos os acessos a dicionários/objetos de dados em `geradores/__init__.py` e `componentes.py`.
    Devem ser capturados padrões como `dados.get("campo")`, `d.get('campo')`, `dados["campo"]` e `d["campo"]`.
    Integrar com o dicionário `ALIASES` de `geradores/_aliases.py` para normalizar os nomes encontrados.
  </action>
  <verify>
    <automated>python _Sistema_Interno/01_Motor_Python/template_checker.py --list-code-fields</automated>
  </verify>
  <done>Lista de campos consumidos pelo código extraída e normalizada corretamente.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar scanner de JSONs e comparador</name>
  <files>_Sistema_Interno/01_Motor_Python/template_checker.py</files>
  <action>
    Adicionar funcionalidade para varrer recursivamente as pastas `0_Modelos_Prontos/` e `_Sistema_Interno/01_Motor_Python/templates/` em busca de arquivos `.json`.
    Para cada JSON, extrair todas as chaves de primeiro nível.
    Implementar o motor de comparação:
    - Campos no código mas ausentes no JSON (Missing).
    - Campos no JSON mas não usados no código (Orphan).
  </action>
  <verify>
    <automated>python _Sistema_Interno/01_Motor_Python/template_checker.py --check-templates</automated>
  </verify>
  <done>Diferenças entre JSONs e código identificadas e mapeadas.</done>
</task>

<task type="auto">
  <name>Task 3: Gerador de relatório e validação de aliases</name>
  <files>_Sistema_Interno/01_Motor_Python/template_checker.py</files>
  <action>
    Finalizar o utilitário com a geração de um relatório formatado no console (usando `colorama` se disponível).
    Adicionar validação de `_aliases.py`: garantir que todos os campos canônicos definidos no dicionário `ALIASES` sejam de fato utilizados no código.
    O relatório deve ser claro sobre quais arquivos JSON estão incompletos.
  </action>
  <verify>
    <automated>python _Sistema_Interno/01_Motor_Python/template_checker.py</automated>
  </verify>
  <done>Relatório de dessincronização gerado com sucesso, incluindo validação de aliases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System | O script lê arquivos Python e JSON do disco. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | template_checker.py | accept | O script é para uso interno do desenvolvedor e não processa dados de usuários externos. |
| T-05-02 | Denial of Service | template_checker.py | mitigate | Limitar a profundidade da varredura recursiva e ignorar pastas como .git ou __pycache__. |
</threat_model>

<verification>
Executar o `template_checker.py` e verificar se ele aponta corretamente campos conhecidos (ex: deletar temporariamente um campo de um JSON de teste e ver se ele reporta como ausente).
</verification>

<success_criteria>
- O script `template_checker.py` é executável.
- O relatório mostra pelo menos uma discrepância real ou confirma 100% de sincronia (se os modelos estiverem perfeitos).
- Erros de importação de `_aliases.py` ou `config.py` são tratados.
</success_criteria>

<output>
Após a conclusão, criar `.gemini/plans/phases/5-templates/05-01-SUMMARY.md`
</output>
