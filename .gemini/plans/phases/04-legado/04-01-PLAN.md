---
phase: 04-legado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _Sistema_Interno/01_Motor_Python/config.py
  - _Sistema_Interno/01_Motor_Python/geradores/__init__.py
  - _Sistema_Interno/01_Motor_Python/compilador.py
  - _Sistema_Interno/01_Motor_Python/json/processo_1065_2026.json
  - _Sistema_Interno/01_Motor_Python/json/processo_6100_Maria.json
autonomous: true
requirements: [FR-02, NFR-01]

must_haves:
  truths:
    - "O motor rejeita tipos de documento com espaços ou caracteres especiais de forma explícita."
    - "Todos os arquivos JSON de teste usam IDs técnicos presentes em config.py."
    - "A geração de documentos continua funcionando para todos os tipos suportados."
  artifacts:
    - path: "_Sistema_Interno/01_Motor_Python/config.py"
      provides: "Mapa de tipos sem aliases legados"
    - path: "_Sistema_Interno/01_Motor_Python/compilador.py"
      provides: "Lógica de despacho limpa e baseada em config"
  key_links:
    - from: "_Sistema_Interno/01_Motor_Python/compilador.py"
      to: "_Sistema_Interno/01_Motor_Python/config.py"
      via: "TIPOS_DOCUMENTO check"
---

<objective>
Eliminar a dívida técnica relacionada ao despacho de documentos, removendo aliases de compatibilidade e lógica de tratamento de strings descritivas ("Alvará de...") em favor de IDs técnicos estritos definidos no config.py.

Propósito: Garantir que o sistema seja previsível e que novos geradores sigam o padrão técnico.
Saída: Código refatorado e arquivos de dados migrados.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.gemini/plans/PROJECT.md
@.gemini/plans/ROADMAP.md
@.gemini/plans/REQUIREMENTS.md
@_Sistema_Interno/01_Motor_Python/config.py
@_Sistema_Interno/01_Motor_Python/compilador.py
@_Sistema_Interno/01_Motor_Python/geradores/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migração de Dados e Saneamento do Config</name>
  <files>_Sistema_Interno/01_Motor_Python/config.py, _Sistema_Interno/01_Motor_Python/json/processo_1065_2026.json, _Sistema_Interno/01_Motor_Python/json/processo_6100_Maria.json</files>
  <action>
    - No arquivo `config.py`, remova a entrada `"regularizacao": "parecer_tecnico"` do dicionário `TIPOS_DOCUMENTO`. 
    - Verifique se `"alvara_regularizacao"` já mapeia para `"parecer_tecnico"` (já deve estar lá).
    - Nos arquivos JSON listados, altere o valor de `"tipo_relatorio"` de `"regularizacao"` para `"alvara_regularizacao"`.
    - Isso garante que os dados de teste usem o ID técnico canônico.
  </action>
  <verify>
    <automated>python -c "from config import TIPOS_DOCUMENTO; assert 'regularizacao' not in TIPOS_DOCUMENTO"</automated>
  </verify>
  <done>Aliases removidos e JSONs atualizados para usar IDs técnicos.</done>
</task>

<task type="auto">
  <name>Task 2: Refatoração do Despacho em geradores/__init__.py</name>
  <files>_Sistema_Interno/01_Motor_Python/geradores/__init__.py</files>
  <action>
    - Na função `gerar(dados, caminho_saida=None)`, mude o valor padrão de `tipo = dados.get("tipo_relatorio", "regular_izacao")` (ou similar) para buscar apenas o campo sem default inseguro.
    - Se o tipo for None ou vazio, lance um `ValueError` explicativo.
    - Na verificação `if not categoria:`, melhore o log para sugerir que o usuário verifique o `config.py` ou os modelos disponíveis.
  </action>
  <verify>
    <automated>python _Sistema_Interno/01_Motor_Python/run_tests.py --motor</automated>
  </verify>
  <done>Função de geração não utiliza mais defaults legados e falha com clareza.</done>
</task>

<task type="auto">
  <name>Task 3: Limpeza de Lógica Descritiva no compilador.py</name>
  <files>_Sistema_Interno/01_Motor_Python/compilador.py</files>
  <action>
    - No loop que processa `pecas_selecionadas` (em `main`), remova o bloco `if " " in peca or "—" in peca:`.
    - Substitua por uma verificação robusta: `if peca not in TIPOS_DOCUMENTO:`.
    - Se a peça for inválida, logue um erro explícito usando `_err` e continue para a próxima peça, em vez de apenas ignorar silenciosamente.
    - Remova comentários que mencionam "Alvará de Regularização... do Gemini" como algo a ser sanitizado manualmente; o sistema agora exige IDs técnicos.
  </action>
  <verify>
    <automated>python _Sistema_Interno/01_Motor_Python/run_tests.py --motor</automated>
  </verify>
  <done>Código de despacho no orquestrador principal simplificado e livre de heurísticas de string.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| JSON Entry | Dados externos (JSON) definem o fluxo de execução através de `tipo_relatorio`. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `tipo_relatorio` | Mitigate | Validar contra `TIPOS_DOCUMENTO` antes de despachar para geradores. |
| T-04-02 | Denial of Service | `gerar()` | Mitigate | Falha controlada com `ValueError` para evitar recursão ou crash inesperado por tipo None. |
</threat_model>

<verification>
1. Executar `python run_tests.py --motor` e garantir 100% de sucesso.
2. Criar um arquivo JSON temporário com `"tipo_relatorio": "Tipo Inexistente"` e verificar se o `compilador.py` emite um erro legível em vez de falhar silenciosamente ou crashar.
</verification>

<success_criteria>
- Zero ocorrências de strings com espaços sendo usadas para controle de fluxo de tipo em `compilador.py`.
- Remoção do alias `"regularizacao"` em `config.py`.
- Suite de testes completa passando.
</success_criteria>

<output>
Após a conclusão, crie `.gemini/plans/phases/04-legado/04-01-SUMMARY.md`
</output>
