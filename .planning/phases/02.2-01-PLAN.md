---
phase: 02.2-refatoracao-motor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "Sistema/motor/generators/componentes/corpo.py",
  "Sistema/motor/ui/preview_html.py",
  "Sistema/motor/generators/geradores_core.py",
  "Sistema/motor/utils/calculadora_indices.py"
]
autonomous: true
requirements: [INT-01]
must_haves:
  truths:
    - "Prefixos 'Considerando que' não são duplicados em textos robustos"
    - "Marcadores [PREENCHER] são suprimidos em campos narrativos quando há conteúdo base"
    - "Preview HTML mantém paridade visual com o DOCX quanto aos prefixos"
    - "Calculadora de índices opera em modo read-only sem alteração silenciosa de dados"
  artifacts:
    - path: "Sistema/motor/generators/componentes/corpo.py"
      provides: "Lógica de prefixo inteligente"
    - path: "Sistema/motor/generators/geradores_core.py"
      provides: "Supressão de marcadores intrusivos"
  key_links:
    - from: "Sistema/motor/generators/componentes/corpo.py"
      to: "Sistema/motor/ui/preview_html.py"
      via: "Sincronização de lógica de prefixo"
---

<objective>
Refatorar o motor Python (Surgical Update) para garantir que a soberania do conteúdo gerado pela IA seja respeitada, eliminando prefixos redundantes e marcadores intrusivos, além de garantir a integridade dos cálculos.

Purpose: Evitar intervenção manual pós-geração e garantir paridade entre preview e documento final.
Output: Motor Python atualizado e validado.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v2.1-fase2-SPEC.md

<interfaces>
From Sistema/motor/generators/componentes/corpo.py:
```python
def build_corpo(doc, dados):
    # Constrói a seção de considerandos
```

From Sistema/motor/generators/geradores_core.py:
```python
def gerar(tipo_doc, dados, output_path):
    # Fluxo principal de geração
```

From Sistema/motor/ui/preview_html.py:
```python
def _html_corpo(dados):
    # Gera HTML para a seção de considerandos
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar Inteligência de Prefixo e Sincronizar Preview</name>
  <files>Sistema/motor/generators/componentes/corpo.py, Sistema/motor/ui/preview_html.py</files>
  <action>
    1. Em `corpo.py`, alterar `build_corpo` para tornar "Considerando que" opcional. 
       - Não injetar se: texto começa com "Considerando", texto começa com letra maiúscula (após limpeza), ou `paragrafo_abertura` estiver presente.
       - Garantir que a primeira letra seja mantida em maiúscula se o prefixo for omitido.
    2. Em `preview_html.py`, sincronizar a lógica na função `_html_corpo` para refletir as mesmas regras de prefixo, garantindo paridade visual.
    Referência: Requisito 2.1 e 2.3 da SPEC.
  </action>
  <verify>
    <automated>python Sistema/motor/scripts/run_tests.py --filter=prefixo</automated>
  </verify>
  <done>DOCX e Preview HTML não apresentam duplicação de prefixos em testes com textos robustos.</done>
</task>

<task type="auto">
  <name>Task 2: Supressão de Marcadores Intrusivos e Auditoria da Calculadora</name>
  <files>Sistema/motor/generators/geradores_core.py, Sistema/motor/utils/calculadora_indices.py</files>
  <action>
    1. Em `geradores_core.py`, na função `gerar()`, impedir a injeção de `[PREENCHER]` em `fundamentacao_legal` e `paragrafos_adicionais` se `paragrafo_abertura` ou `considerandos` tiverem conteúdo substancial.
    2. Em `calculadora_indices.py`, auditar a função `calcular()` para garantir que ela não modifique o dicionário `dados` original (read-only mode).
    Referência: Requisito 2.2 e 2.4 da SPEC.
  </action>
  <verify>
    <automated>python Sistema/motor/scripts/run_tests.py --filter=marcadores</automated>
  </verify>
  <done>Marcadores [PREENCHER] suprimidos em campos de narrativa; Calculadora validada como read-only.</done>
</task>

<task type="auto">
  <name>Task 3: Validação End-to-End com Processo Real</name>
  <files>Entrada/processo_3425_2026.json</files>
  <action>
    Executar o pipeline completo usando o arquivo `processo_3425_2026.json` (que deve conter textos robustos e desafios de fundamentação).
    Verificar:
    - Ausência de prefixos duplicados.
    - Ausência de [PREENCHER] indesejados.
    - Preservação dos valores da calculadora no documento final.
  </action>
  <verify>
    <automated>python Sistema/motor/generators/compilador.py Entrada/processo_3425_2026.json</automated>
  </verify>
  <done>Documento gerado com sucesso respeitando todas as regras da Fase 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Motor -> DOCX | Conteúdo gerado pela IA é inserido no documento. O motor deve garantir que metacaracteres não quebrem a estrutura. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-01 | Integrity | geradores_core.py | mitigate | Validar que a supressão de marcadores não oculte campos críticos de identificação. |
| T-02.2-02 | Integrity | calculadora_indices.py | mitigate | Garantir modo read-only para evitar que divergências técnicas sejam "corrigidas" incorretamente no documento. |
</threat_model>

<verification>
- Execução de `Sistema/motor/scripts/run_tests.py`.
- Inspeção visual do DOCX gerado para o `processo_3425_2026.json`.
- Comparação do Preview HTML com o DOCX.
</verification>

<success_criteria>
- Motor Python atualizado sem quebrar compatibilidade com templates antigos.
- Paridade visual de 100% entre Preview e DOCX quanto aos considerandos.
- Zero injeções indesejadas de [PREENCHER] em pareceres com fundamentação robusta.
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-refatoracao-motor/02.2-01-SUMMARY.md`
</output>
