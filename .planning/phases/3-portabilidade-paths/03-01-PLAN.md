---
phase: 3-portabilidade-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _Sistema_Interno/01_Motor_Python/config.py
  - _Sistema_Interno/01_Motor_Python/compilador.py
  - _Sistema_Interno/01_Motor_Python/geradores/__init__.py
  - _Sistema_Interno/01_Motor_Python/analisador_pdf.py
  - _Sistema_Interno/01_Motor_Python/extrator_pdf.py
  - _Sistema_Interno/01_Motor_Python/modo_pdf.py
  - _Sistema_Interno/01_Motor_Python/painel_gem.py
  - _Sistema_Interno/01_Motor_Python/compilador_livre.py
  - _Sistema_Interno/01_Motor_Python/preview_html.py
  - _Sistema_Interno/01_Motor_Python/refinador.py
autonomous: true
requirements: ["FR-01", "NFR-01", "NFR-02"]
must_haves:
  truths:
    - "config.py define PROJECT_ROOT, PASTA_ENTRADA, PASTA_SAIDA, PASTA_MODELOS e PASTA_TREINO"
    - "Nenhum arquivo .py contém as strings literais '1_Colar_JSON_Aqui', '2_Documentos_Prontos', '0_Modelos_Prontos' ou '3_Treinar_Inteligencia' fora de config.py"
  artifacts:
    - path: "_Sistema_Interno/01_Motor_Python/config.py"
      provides: "Fonte única de verdade para nomes de pastas do projeto"
  key_links: []
---

<objective>
Centralizar as constantes de nome de pasta em config.py, eliminando strings literais duplicadas em 8+ arquivos.

Purpose: Os nomes das pastas do projeto ("1_Colar_JSON_Aqui", "2_Documentos_Prontos", etc.) aparecem como strings literais espalhadas por múltiplos arquivos. Se uma pasta for renomeada, é necessário alterar manualmente cada ocorrência. A pesquisa confirmou que não há paths absolutos hardcoded — o problema real é a duplicação de strings de nome de pasta.

Output: config.py passa a ser a fonte única de verdade para nomes de pastas. Todos os outros módulos importam e usam essas constantes.

Nota: A concern original ("Portabilidade") foi investigada e confirmada como já resolvida (todos os arquivos usam __file__-relative paths). O escopo foi ajustado para o problema real identificado.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@_Sistema_Interno/01_Motor_Python/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar constantes de pasta em config.py</name>
  <files>_Sistema_Interno/01_Motor_Python/config.py</files>
  <read_first>_Sistema_Interno/01_Motor_Python/config.py</read_first>
  <action>
Adicionar na seção "CAMINHOS" de config.py, após a linha de PROJECT_DIR:

```python
PROJECT_ROOT    = os.path.dirname(os.path.dirname(SCRIPT_DIR))  # pasta raiz: 02. Pareceres
PASTA_ENTRADA   = os.path.join(PROJECT_ROOT, "1_Colar_JSON_Aqui")
PASTA_SAIDA     = os.path.join(PROJECT_ROOT, "2_Documentos_Prontos")
PASTA_MODELOS   = os.path.join(PROJECT_ROOT, "0_Modelos_Prontos")
PASTA_TREINO    = os.path.join(PROJECT_ROOT, "3_Treinar_Inteligencia")
```

ATENÇÃO: config.py já tem PROJECT_DIR = os.path.dirname(SCRIPT_DIR) que aponta para _Sistema_Interno/. O novo PROJECT_ROOT deve ser os.path.dirname(os.path.dirname(SCRIPT_DIR)) que aponta para 02. Pareceres/ (onde ficam as pastas de usuário).
  </action>
  <verify>python -c "from config import PASTA_ENTRADA, PASTA_SAIDA, PASTA_MODELOS, PASTA_TREINO; print('OK')"</verify>
  <acceptance_criteria>
    - config.py contém PROJECT_ROOT, PASTA_ENTRADA, PASTA_SAIDA, PASTA_MODELOS, PASTA_TREINO
  </acceptance_criteria>
  <done>Constantes adicionadas em config.py.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar compilador.py</name>
  <files>_Sistema_Interno/01_Motor_Python/compilador.py</files>
  <read_first>_Sistema_Interno/01_Motor_Python/compilador.py</read_first>
  <action>
Em compilador.py:
1. Adicionar na linha de imports do config: `from config import TIPOS_DOCUMENTO, PASTA_ENTRADA, PASTA_SAIDA`
2. Localizar a linha com `os.path.join(os.path.dirname(os.path.dirname(SCRIPT_DIR)), "1_Colar_JSON_Aqui")` e substituir por `PASTA_ENTRADA`
3. Localizar qualquer referência a `"2_Documentos_Prontos"` e substituir pela constante importada
  </action>
  <verify>grep -n "1_Colar_JSON_Aqui\|2_Documentos_Prontos" _Sistema_Interno/01_Motor_Python/compilador.py</verify>
  <acceptance_criteria>
    - grep retorna zero resultados com strings literais de pasta
  </acceptance_criteria>
  <done>compilador.py usa constantes de config.py.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar geradores/__init__.py</name>
  <files>_Sistema_Interno/01_Motor_Python/geradores/__init__.py</files>
  <read_first>_Sistema_Interno/01_Motor_Python/geradores/__init__.py</read_first>
  <action>
Em geradores/__init__.py:
1. Garantir que `PASTA_SAIDA` seja importado de config (via `from config import ... PASTA_SAIDA`)
2. Localizar a linha com `os.path.join(root_dir, "2_Documentos_Prontos", nome_pasta)` e substituir por `os.path.join(PASTA_SAIDA, nome_pasta)`
3. Remover a variável `root_dir` se não for mais usada após essa substituição
4. Localizar `"0_Modelos_Prontos"` e substituir por `PASTA_MODELOS` (importando também)
  </action>
  <verify>grep -n "2_Documentos_Prontos\|0_Modelos_Prontos\|1_Colar_JSON_Aqui" _Sistema_Interno/01_Motor_Python/geradores/__init__.py</verify>
  <acceptance_criteria>
    - grep retorna zero resultados com strings literais de pasta
  </acceptance_criteria>
  <done>geradores/__init__.py usa constantes de config.py.</done>
</task>

<task type="auto">
  <name>Task 4: Atualizar demais módulos (analisador_pdf, extrator_pdf, modo_pdf, painel_gem, compilador_livre, preview_html, refinador)</name>
  <files>
    _Sistema_Interno/01_Motor_Python/analisador_pdf.py,
    _Sistema_Interno/01_Motor_Python/extrator_pdf.py,
    _Sistema_Interno/01_Motor_Python/modo_pdf.py,
    _Sistema_Interno/01_Motor_Python/painel_gem.py,
    _Sistema_Interno/01_Motor_Python/compilador_livre.py,
    _Sistema_Interno/01_Motor_Python/preview_html.py,
    _Sistema_Interno/01_Motor_Python/refinador.py
  </files>
  <read_first>Cada arquivo antes de editar.</read_first>
  <action>
Para cada arquivo:
1. Ler o arquivo
2. Identificar quais constantes de pasta são usadas (1_Colar_JSON_Aqui, 2_Documentos_Prontos, 0_Modelos_Prontos, 3_Treinar_Inteligencia)
3. Adicionar import das constantes necessárias de config.py
4. Substituir as strings literais pelas constantes importadas
5. Remover cálculo de PROJECT_ROOT local se o arquivo o tiver e não for mais necessário

IMPORTANTE: Alguns arquivos calculam PROJECT_ROOT localmente com os.path.dirname(os.path.dirname(...)). Após importar de config.py, essa linha local pode ser removida.
  </action>
  <verify>
grep -rn "1_Colar_JSON_Aqui\|2_Documentos_Prontos\|0_Modelos_Prontos\|3_Treinar_Inteligencia" _Sistema_Interno/01_Motor_Python/ --include="*.py" | grep -v config.py | grep -v ".pyc"
  </verify>
  <acceptance_criteria>
    - grep acima retorna zero resultados
  </acceptance_criteria>
  <done>Todos os módulos usam constantes de config.py.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python -m py_compile _Sistema_Interno/01_Motor_Python/config.py
- [ ] python -m py_compile _Sistema_Interno/01_Motor_Python/compilador.py
- [ ] python -m py_compile _Sistema_Interno/01_Motor_Python/geradores/__init__.py
- [ ] python run_tests.py --motor  (deve passar 100% — verifica que nenhuma refatoração quebrou a geração)
- [ ] grep -rn "1_Colar_JSON_Aqui\|2_Documentos_Prontos\|0_Modelos_Prontos\|3_Treinar_Inteligencia" _Sistema_Interno/01_Motor_Python/ --include="*.py" | grep -v "config.py"  (deve retornar zero)
</verification>

<success_criteria>
- Todas as tasks completadas
- Zero strings literais de pasta fora de config.py
- run_tests.py --motor passa 100%
- py_compile limpo nos módulos principais
</success_criteria>

<output>
After completion, create `.planning/phases/3-portabilidade-paths/03-01-SUMMARY.md`
</output>
