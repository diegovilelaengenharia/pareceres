<!-- generated-by: gsd-doc-writer -->
# Especificação Técnica - Fase 2: Refatoração do Motor (Surgical Update)

## 1. Visão Geral
Esta fase foca em ajustar o comportamento do motor Python para garantir a soberania do conteúdo gerado pelo Gemini (Fase 1). O motor deve deixar de ser "autoritário" em relação a prefixos e campos vazios quando a IA já forneceu uma estrutura de texto robusta e consolidada. O objetivo é reduzir a necessidade de edição manual após a geração do documento.

## 2. Requisitos Técnicos

### 2.1. Desativação da Injeção de "Considerando que"
**Arquivo:** `Sistema/motor/generators/componentes/corpo.py`

**Contexto Atual:** O motor força o negrito **Considerando que** no início de cada item da lista de considerandos, realizando uma limpeza de strings que pode ser redundante se a IA já formatou o texto.

**Mudanças Necessárias:**
- Alterar a função `build_corpo` para aprimorar a detecção de "Inteligência de Prefixo".
- **Lógica de Robustez:** Não injetar o prefixo `add_run(p, "Considerando que ", bold=True...)` se:
    1. O texto original já começar com "Considerando" (verificação feita antes da remoção por regex).
    2. O texto (após limpeza) começar com letra maiúscula (indicando uma frase completa/independente).
    3. O campo `paragrafo_abertura` estiver presente e preenchido (sinalizando que a IA optou por uma narrativa fluida).
    4. O item já possuir marcadores técnicos ou numeração (já implementado, mas deve ser mantido).
- Garantir que a primeira letra seja mantida em maiúscula caso o prefixo não seja injetado.

### 2.2. Supressão de Marcadores `[PREENCHER]` Intrusivos
**Arquivo:** `Sistema/motor/generators/geradores_core.py`

**Contexto Atual:** O validador de integridade injeta marcadores de preenchimento em quase todos os campos faltantes definidos no template.

**Mudanças Necessárias:**
- Refinar a lógica na função `gerar()`, especificamente na seção que trata `campos_faltantes`.
- Implementar a regra: Se `paragrafo_abertura` **OU** `considerandos` possuírem conteúdo substancial, o motor **NÃO** deve injetar marcadores `[PREENCHER]` nos seguintes campos de texto:
    - `fundamentacao_legal`
    - `paragrafos_adicionais`
- Manter a injeção apenas para campos críticos de identificação (número do processo, logradouro, requerente, etc.) e campos técnicos essenciais (`documentos_emitir`).

### 2.3. Sincronização do Preview HTML
**Arquivo:** `Sistema/motor/ui/preview_html.py`

**Contexto Atual:** A função `_html_corpo` utiliza uma lógica de prefixos fixa, diferente da aplicada no gerador DOCX.

**Mudanças Necessárias:**
- Sincronizar a lógica de exibição de considerandos com as novas regras do `corpo.py`.
- O preview deve detectar a robustez do texto e a presença de `paragrafo_abertura` para decidir se exibe o `<span>` com a classe `kw-considerando`.
- O objetivo é garantir 100% de paridade visual entre o preview e o documento final.

### 2.4. Calculadora Silenciosa (Read-only)
**Arquivo:** `Sistema/motor/utils/calculadora_indices.py`

**Contexto Atual:** A calculadora valida os índices e emite relatórios de erro e divergência.

**Mudanças Necessárias:**
- Garantir que a função `calcular()` atue de forma estritamente analítica.
- Verificar se em algum ponto a calculadora modifica o dicionário `dados` original.
- Reforçar que as divergências encontradas devem ser reportadas apenas via `BaseEngine.log_report` e incluídas no retorno da função para exibição no preview, sem nunca realizar "auto-correção" nos valores de área ou índices que serão impressos no documento.

## 3. Critérios de Aceite
- [ ] Documentos gerados com `paragrafo_abertura` robusto não apresentam duplicação de prefixos nos considerandos.
- [ ] Campos de narrativa vazios no JSON não exibem `[PREENCHER]` se houver outro texto de fundamentação presente.
- [ ] O Preview HTML reflete exatamente as mesmas regras de prefixo e visibilidade do DOCX.
- [ ] A Calculadora de Índices reporta erros de TO/CA/TP no console e logs, mas o documento final preserva os valores exatos enviados pelo Gemini.

## 4. Plano de Testes
1. **Cenário A (Soberania):** Enviar JSON com `paragrafo_abertura` e considerandos que começam com letra maiúscula. Validar que o DOCX não injeta "Considerando que" em negrito.
2. **Cenário B (Limpeza):** Enviar JSON com `considerandos` vazios e `paragrafo_abertura` preenchido. Validar que não aparece `[PREENCHER: considerandos]`.
3. **Cenário C (Preview):** Comparar lado a lado o HTML gerado e o DOCX para um caso complexo de fundamentação legal.
4. **Cenário D (Calculadora):** Simular divergência de área entre `areas_matriz` e `area_total_construida`. Validar que o erro aparece nos logs mas o valor no DOCX permanece o informado pela IA.