---
phase: 05-qualidade-e-consistencia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _Sistema_Interno/01_Motor_Python/scripts/template_checker.py
  - _Sistema_Interno/01_Motor_Python/scripts/run_tests.py
autonomous: true
requirements: [FR-03, NFR-01]
must_haves:
  truths:
    - "Modelos em 0_Modelos_Prontos/ são validados contra o schema_validator.py automaticamente"
    - "Campos obrigatórios definidos em templates/*.json são verificados nos modelos públicos"
    - "O relatório aponta exatamente quais chaves estão faltando nos exemplos para o usuário"
  artifacts:
    - path: "_Sistema_Interno/01_Motor_Python/scripts/template_checker.py"
      provides: "Utilitário de auditoria de consistência entre motor e modelos"
  key_links:
    - from: "scripts/template_checker.py"
      to: "core/schema_validator.py"
      via: "importação da função validar()"
    - from: "scripts/template_checker.py"
      to: "0_Modelos_Prontos/"
      via: "escaneamento de arquivos JSON"
---

<objective>
Implementar o Detector de Dessincronização de Templates para garantir que os modelos JSON fornecidos ao usuário (em 0_Modelos_Prontos/) estejam 100% sincronizados com a lógica do motor e as exigências do schema_validator.

Propósito: Eliminar o risco de modelos obsoletos gerarem documentos com dados faltantes.
Saída: Script `template_checker.py` integrado à suíte de testes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@_Sistema_Interno/01_Motor_Python/core/schema_validator.py
@_Sistema_Interno/01_Motor_Python/generators/templates/_esquema_base.json
@_Sistema_Interno/01_Motor_Python/scripts/run_tests.py
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Implementação do template_checker.py</name>
  <files>_Sistema_Interno/01_Motor_Python/scripts/template_checker.py</files>
  <action>
    Criar o script `template_checker.py` que:
    1. Importa `validar` de `core.schema_validator`.
    2. Localiza a pasta `0_Modelos_Prontos/` (relativa ao script).
    3. Para cada arquivo `MODELO_*.json`:
       a. Executa `validar(dados)`.
       b. Carrega o template correspondente em `generators/templates/{tipo_relatorio}.json`.
       c. Verifica se todas as chaves em `campos_obrigatorios` do template estão presentes no modelo.
    4. Gera um relatório consolidado de erros (bloqueantes) e avisos (campos opcionais faltando).
    5. Retorna exit code 1 se houver erros em qualquer modelo.
  </action>
  <verify>
    <automated>python _Sistema_Interno/01_Motor_Python/scripts/template_checker.py</automated>
  </verify>
  <done>Script operacional detectando discrepâncias nos modelos.</done>
</task>

<task type="auto">
  <name>Task 5.2: Integração com Suite de Testes</name>
  <files>_Sistema_Interno/01_Motor_Python/scripts/run_tests.py</files>
  <action>
    Atualizar o script `run_tests.py` para incluir uma nova etapa: "Verificação de Modelos Públicos".
    Deve chamar `template_checker.py` e falhar o build/teste geral se os modelos estiverem dessincronizados.
  </action>
  <verify>
    <automated>python _Sistema_Interno/01_Motor_Python/scripts/run_tests.py</automated>
  </verify>
  <done>Modelos públicos protegidos por testes automatizados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 0_Modelos_Prontos → Motor | Confiança de que arquivos na pasta raiz seguem o schema interno. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | MODELO_*.json | mitigate | O checker detecta alterações acidentais ou malformações nos modelos públicos. |
</threat_model>

<verification>
1. Introduzir um erro proposital em `MODELO_01_Alvara_Aprovacao.json` (ex: renomear uma chave obrigatória).
2. Executar `python _Sistema_Interno/01_Motor_Python/scripts/template_checker.py`.
3. Verificar se o script detecta o erro e aponta a chave correta.
4. Corrigir o erro e verificar se o script passa (exit code 0).
</verification>

<success_criteria>
- 100% dos arquivos em `0_Modelos_Prontos/` são validados automaticamente.
- O motor não permite "testes aprovados" se os modelos públicos estiverem quebrados.
</success_criteria>

<output>
Após a conclusão, crie `.planning/phases/05-qualidade-e-consistencia/05-01-SUMMARY.md`
</output>
