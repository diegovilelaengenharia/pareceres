---
phase: 2-fortalecimento-testes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [_Sistema_Interno/01_Motor_Python/logger.py, _Sistema_Interno/01_Motor_Python/compilador.py, _Sistema_Interno/01_Motor_Python/schema_validator.py, _Sistema_Interno/01_Motor_Python/run_tests.py]
autonomous: true
requirements: ["Fortalecimento de Validação e Testes"]
must_haves:
  truths:
    - "A aplicação gera logs em um arquivo físico (motor.log)"
    - "O validador de schemas capta erros de tipo em propriedades que deveriam ser strings mas são booleanas/números"
    - "A suíte de testes também valida se arquivos JSON inválidos falham conforme esperado"
  artifacts:
    - path: "_Sistema_Interno/01_Motor_Python/logger.py"
      provides: "Configuração global do módulo de log nativo do Python com suporte a Colorama"
  key_links: []
---

<objective>
Elevar a robustez e transparência da validação e testes do motor.

Purpose: O sistema hoje usa `print` na tela para avisos e erros usando o Colorama, sem persistência, dificultando a auditoria. Além disso, o validador de schema precisa ser mais rigoroso para blindar o gerador final (python-docx).
Output: Arquivo de logging criado, módulos injetando logs no arquivo `motor.log` em vez de apenas print, e validador mais forte testado contra falhas conhecidas.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@_Sistema_Interno/01_Motor_Python/compilador.py
@_Sistema_Interno/01_Motor_Python/schema_validator.py
@_Sistema_Interno/01_Motor_Python/run_tests.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar Módulo de Log Centralizado</name>
  <files>_Sistema_Interno/01_Motor_Python/logger.py, _Sistema_Interno/01_Motor_Python/compilador.py</files>
  <read_first>_Sistema_Interno/01_Motor_Python/compilador.py</read_first>
  <action>Criar `logger.py` encapsulando a biblioteca nativa `logging`. Deve configurar um FileHandler para `motor.log` e um StreamHandler mantendo a formatação colorida (Colorama) existente no compilador.py. Refatorar o `compilador.py` para usar este logger em vez de definições de _ok(), _warn(), e _err() locais.</action>
  <verify>grep "import logging" _Sistema_Interno/01_Motor_Python/logger.py</verify>
  <acceptance_criteria>
    - `logger.py` has `logging.getLogger` and `logging.FileHandler`
    - `compilador.py` imports `logger` and uses it instead of manual `_ok()`, `_err()` definitions
  </acceptance_criteria>
  <done>Módulo de log criado e integrado ao compilador.</done>
</task>

<task type="auto">
  <name>Task 2: Fortalecer schema_validator.py</name>
  <files>_Sistema_Interno/01_Motor_Python/schema_validator.py</files>
  <read_first>_Sistema_Interno/01_Motor_Python/schema_validator.py</read_first>
  <action>Adicionar verificação de tipo para campos textuais chaves. Ex: "requerente", "numero_processo" e "paragrafo_abertura" devem ser obrigatoriamente strings. Se o JSON contiver um número ou array nessas chaves (onde não for array), o validador deve injetar um erro. Também substituir prints diretos pelo uso do novo logger.</action>
  <verify>grep "isinstance(" _Sistema_Interno/01_Motor_Python/schema_validator.py</verify>
  <acceptance_criteria>
    - `schema_validator.py` checks if string fields are actually `isinstance(val, str)`
  </acceptance_criteria>
  <done>Validação de tipos primitivos adicionada no schema.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar suíte de testes (run_tests.py)</name>
  <files>_Sistema_Interno/01_Motor_Python/run_tests.py</files>
  <read_first>_Sistema_Interno/01_Motor_Python/run_tests.py</read_first>
  <action>Modificar `run_tests.py` para identificar e processar casos negativos. Arquivos com prefixo `TESTE-INVALIDO-*.json` devem FALHAR o schema. O teste é considerado bem-sucedido SE E SOMENTE SE o `schema_validator` retornar erro bloqueante (provando que a validação está funcionando).</action>
  <verify>grep "TESTE-INVALIDO" _Sistema_Interno/01_Motor_Python/run_tests.py</verify>
  <acceptance_criteria>
    - `run_tests.py` separates invalid cases and expects `passou == False` from them
  </acceptance_criteria>
  <done>Casos de uso negativos mapeados na suíte de testes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Executar `python -m py_compile _Sistema_Interno/01_Motor_Python/logger.py`
- [ ] Executar `python -m py_compile _Sistema_Interno/01_Motor_Python/schema_validator.py`
- [ ] Executar `python -m py_compile _Sistema_Interno/01_Motor_Python/run_tests.py`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/2-fortalecimento-testes/02-01-SUMMARY.md`
</output>
