---
phase: 06-excelencia-arquitetural
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _Sistema_Interno/01_Motor_Python/gerar_esquema_base.py
  - _Sistema_Interno/01_Motor_Python/schema_validator.py
  - _Sistema_Interno/01_Motor_Python/painel_gem.py
  - _Sistema_Interno/01_Motor_Python/base_engine.py
  - _Sistema_Interno/01_Motor_Python/calculadora_indices.py
  - _Sistema_Interno/01_Motor_Python/verificador_multas.py
autonomous: true
requirements: [FR-03, NFR-01]
must_haves:
  truths:
    - "Templates novos são detectados automaticamente sem alterar código Python"
    - "Lógica de extração numérica é idêntica em todos os motores de cálculo"
    - "Erros de validação do JSON aparecem de forma clara no painel web"
  artifacts:
    - path: "_Sistema_Interno/01_Motor_Python/templates/_esquema_base.json"
      provides: "Lista consolidada de todas as chaves JSON válidas"
    - path: "_Sistema_Interno/01_Motor_Python/base_engine.py"
      provides: "Classe base para utilitários de cálculo e formatação"
  key_links:
    - from: "calculadora_indices.py"
      to: "base_engine.py"
      via: "importação de utilitários"
    - from: "painel_gem.py"
      to: "motor.json.log"
      via: "leitura de erros estruturados"
---

<objective>
Elevar a qualidade arquitetural do motor através da abstração de lógica comum, sincronização dinâmica de schemas de templates e implementação de observabilidade no painel administrativo.

Propósito: Reduzir dívida técnica, facilitar a adição de novos tipos de documentos e permitir auditoria rápida de falhas via interface web.
Saída: Script de sincronização de schema, BaseEngine compartilhado e Painel Web atualizado com logs estruturados.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@_Sistema_Interno/01_Motor_Python/schema_validator.py
@_Sistema_Interno/01_Motor_Python/calculadora_indices.py
@_Sistema_Interno/01_Motor_Python/verificador_multas.py
@_Sistema_Interno/01_Motor_Python/painel_gem.py
@_Sistema_Interno/01_Motor_Python/logger.py
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Sincronização Dinâmica de Schema</name>
  <files>_Sistema_Interno/01_Motor_Python/gerar_esquema_base.py, _Sistema_Interno/01_Motor_Python/schema_validator.py, _Sistema_Interno/01_Motor_Python/painel_gem.py</files>
  <action>
    1. Criar `gerar_esquema_base.py` que varre `templates/*.json`, extrai todas as chaves únicas e gera `templates/_esquema_base.json`.
    2. Atualizar `schema_validator.py` para carregar esse arquivo e validar chaves de entrada contra ele (substituindo listas hardcoded onde aplicável).
    3. Atualizar `painel_gem.py` (método `api_salvar_json`) para usar as chaves do `_esquema_base.json` na sua lista de 'chaves_conhecidas'.
  </action>
  <verify>
    <automated>python _Sistema_Interno/01_Motor_Python/gerar_esquema_base.py && python _Sistema_Interno/01_Motor_Python/schema_validator.py _Sistema_Interno/01_Motor_Python/json/teste_gemma.json</automated>
  </verify>
  <done>Templates sincronizados e validador usando o arquivo dinâmico.</done>
</task>

<task type="auto">
  <name>Task 6.2: Abstração de Motores (BaseEngine)</name>
  <files>_Sistema_Interno/01_Motor_Python/base_engine.py, _Sistema_Interno/01_Motor_Python/calculadora_indices.py, _Sistema_Interno/01_Motor_Python/verificador_multas.py</files>
  <action>
    1. Criar `base_engine.py` com classe `BaseEngine` contendo métodos estáticos: `parse_number(text)` (extração de float), `format_currency(value)` (BRL), e `log_report(level, msg, data)` (wrapper para logger.py).
    2. Refatorar `calculadora_indices.py` e `verificador_multas.py` para remover suas funções locais `_num` e utilizar `BaseEngine`.
    3. Padronizar as mensagens de erro/aviso para incluírem metadados estruturados para o JSON log.
  </action>
  <verify>
    <automated>python _Sistema_Interno/01_Motor_Python/run_tests.py</automated>
  </verify>
  <done>Código duplicado removido e motores utilizando utilitários centralizados.</done>
</task>

<task type="auto">
  <name>Task 6.3: Painel JSON-Aware (Observabilidade)</name>
  <files>_Sistema_Interno/01_Motor_Python/painel_gem.py</files>
  <action>
    1. Implementar `api_logs()` em `painel_gem.py` que lê as últimas 50 linhas de `motor.json.log`, filtra por 'ERROR' ou 'WARNING'.
    2. Atualizar o frontend (no `painel_gem.py` ou `painel_gem.html`) para exibir esses alertas na aba "Registro do Sistema" de forma tabular e legível.
    3. Garantir que erros de validação do `schema_validator.py` (quando disparados via painel) sejam capturados e exibidos imediatamente.
  </action>
  <verify>
    <automated>python _Sistema_Interno/01_Motor_Python/painel_gem.py --test-logs</automated>
  </verify>
  <done>Interface web exibindo logs de erro estruturados vindos do motor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Templates → Schema | Confiança nos arquivos JSON de templates para gerar o esquema de validação. |
| Web UI → API | O painel web consome logs do servidor local. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | _esquema_base.json | mitigate | Gerar o arquivo via script autenticado, não permitir edição manual via UI. |
| T-06-02 | Information Disclosure | motor.json.log | mitigate | O painel só expõe logs se acessado via localhost (padrão do sistema). |
</threat_model>

<verification>
- Executar `gerar_esquema_base.py` e verificar se `_esquema_base.json` contém chaves de templates novos.
- Executar `calculadora_indices.py` e `verificador_multas.py` standalone para garantir que o refactoring não quebrou a lógica.
- Acessar o Painel GEM e verificar se os logs aparecem na aba de registros.
</verification>

<success_criteria>
- Sincronização automática de chaves sem hardcoding.
- Redução de duplicidade de código nos motores de cálculo.
- Visibilidade de erros do motor diretamente na interface administrativa.
</success_criteria>

<output>
Após a conclusão, crie `.planning/phases/06-excelencia-arquitetural/06-01-SUMMARY.md`
</output>
