---
phase: 14-certidoes-separadas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "Sistema/motor/core/config.py",
  "Sistema/motor/generators/compilador.py",
  "Sistema/motor/ui/preview_html.py",
  "Sistema/modelos/MODELO_16_Certidoes_Separadas.json"
]
autonomous: true
requirements: [FR-01, FR-02, ARCH-01]
must_haves:
  truths:
    - "O sistema reconhece o tipo 'certidoes_separadas_localizacao_confrontacao' no config.py"
    - "O compilador gera automaticamente 3 arquivos (Parecer, Localização, Confrontação) para o novo tipo mestre"
    - "O preview HTML exibe a lista completa de documentos que serão emitidos"
    - "O modelo JSON 16 está configurado com a estrutura correta para emissão múltipla"
  artifacts:
    - path: "Sistema/modelos/MODELO_16_Certidoes_Separadas.json"
      provides: "Modelo de referência para múltiplas certidões"
    - path: "Sistema/motor/core/config.py"
      provides: "Mapeamento dos novos tipos de documentos"
  key_links:
    - from: "Sistema/motor/generators/compilador.py"
      to: "Sistema/motor/core/config.py"
      via: "TIPOS_DOCUMENTO mapping"
    - from: "Sistema/motor/generators/compilador.py"
      to: "Sistema/motor/ui/preview_html.py"
      via: "gerar_preview call"
---

<objective>
Implementar o suporte à geração de múltiplas certidões (Localização e Confrontação) e um Parecer Técnico a partir de um único arquivo JSON de entrada.

Objetivo: Automatizar o fluxo de trabalho para processos que exigem peças técnicas separadas, eliminando a necessidade de rodar o motor múltiplas vezes manualmente.
Saída: Novo tipo de relatório mestre, novos mapeamentos de tipos e UI de preview atualizada.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Sistema/motor/core/config.py
@Sistema/motor/generators/compilador.py
@Sistema/motor/ui/preview_html.py
</context>

<tasks>

<task type="auto">
  <name>Configurar novos tipos de documentos no config.py</name>
  <files>Sistema/motor/core/config.py</files>
  <action>
    - Adicionar "certidao_confrontacao": "parecer_simples" ao dicionário TIPOS_DOCUMENTO.
    - Adicionar "certidoes_separadas_localizacao_confrontacao": "parecer_tecnico" ao dicionário TIPOS_DOCUMENTO como o tipo mestre (bundled type).
    - Verificar se "certidao_localizacao" já está mapeado corretamente (deve ser "parecer_simples").
  </action>
  <verify>
    <automated>python -c "from Sistema.motor.core.config import TIPOS_DOCUMENTO; assert 'certidao_confrontacao' in TIPOS_DOCUMENTO; assert 'certidoes_separadas_localizacao_confrontacao' in TIPOS_DOCUMENTO"</automated>
  </verify>
  <done>Tipos de documentos registrados e prontos para uso pelo despachante.</done>
</task>

<task type="auto">
  <name>Criar Modelo JSON 16 para Certidões Separadas</name>
  <files>Sistema/modelos/MODELO_16_Certidoes_Separadas.json</files>
  <action>
    - Criar o arquivo baseado no MODELO_01_Alvara_Aprovacao.json ou similar.
    - Definir "tipo_relatorio": "certidoes_separadas_localizacao_confrontacao".
    - Configurar a lista "documentos_emitir" com: ["parecer_tecnico", "certidao_localizacao", "certidao_confrontacao"].
    - Adicionar campos de exemplo para Maria Lúcia (Processo 4467/2026) conforme o contexto do usuário.
  </action>
  <verify>
    <automated>python -c "import json; d = json.load(open('Sistema/modelos/MODELO_16_Certidoes_Separadas.json', encoding='utf-8')); assert d['tipo_relatorio'] == 'certidoes_separadas_localizacao_confrontacao'; assert len(d['documentos_emitir']) == 3"</automated>
  </verify>
  <done>Modelo de referência criado com a estrutura de emissão múltipla.</done>
</task>

<task type="auto">
  <name>Aprimorar Lógica de Bundling no Compilador</name>
  <files>Sistema/motor/generators/compilador.py</files>
  <action>
    - Modificar a função main() ou _relatorio_prevoo para interceptar o tipo "certidoes_separadas_localizacao_confrontacao".
    - Se o tipo mestre for detectado e "documentos_emitir" estiver vazio ou ausente nos dados, injetar automaticamente a lista padrão: ["parecer_tecnico", "certidao_localizacao", "certidao_confrontacao"].
    - Garantir que a lógica de iteração existente em compilador.py (já presente) processe cada item da lista individualmente.
  </action>
  <verify>
    <automated>python -c "import json, os; from Sistema.motor.generators import compilador; # Teste unitário manual ou via script de teste se disponível"</automated>
  </verify>
  <done>Compilador capaz de orquestrar a geração do pacote de documentos automaticamente.</done>
</task>

<task type="auto">
  <name>Atualizar UI de Preview para Múltiplas Peças</name>
  <files>Sistema/motor/ui/preview_html.py</files>
  <action>
    - Modificar gerar_preview para verificar se dados['documentos_emitir'] contém múltiplas peças.
    - Adicionar um banner ou badge visual no topo do HTML indicando "GERAÇÃO EM LOTE DETECTADA".
    - Exibir uma lista clara de todos os arquivos DOCX que serão criados na seção de Documentos a Emitir.
  </action>
  <verify>
    <automated>grep -i "GERAÇÃO EM LOTE" Sistema/motor/ui/preview_html.py</automated>
  </verify>
  <done>Interface de preview reflete a intenção de gerar múltiplos arquivos, dando segurança ao usuário.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON Input → Compilador | Dados não sanitizados do JSON podem afetar nomes de arquivos ou caminhos de saída. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | compilador.py | mitigate | Validar o tipo_relatorio contra TIPOS_DOCUMENTO antes de iniciar a iteração. |
| T-14-02 | Information Disclosure | preview_html.py | mitigate | Garantir que arquivos temporários de preview sejam criados em diretórios seguros do sistema. |
</threat_model>

<verification>
1. Rodar o motor com o `MODELO_16` na pasta de Entrada.
2. Confirmar que o preview HTML mostra os 3 documentos.
3. Confirmar que na pasta de Saída foram criados 3 arquivos DOCX distintos e válidos.
</verification>

<success_criteria>
- O sistema gera 3 arquivos DOCX a partir de um único JSON quando o tipo 'certidoes_separadas_localizacao_confrontacao' é usado.
- O preview HTML lista corretamente os 3 documentos planejados.
- Não há regressão na geração de documentos simples (um por vez).
</success_criteria>

<output>
After completion, create `.planning/phases/14-certidoes-separadas/14-01-SUMMARY.md`
</output>
