---
phase: 16-refatoracao-gerador-docx
plan: 03
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - Sistema/motor/generators/componentes/__init__.py
  - Sistema/motor/generators/componentes/tabelas.py
  - Sistema/motor/generators/componentes/corpo.py
  - Sistema/motor/generators/componentes/conclusao.py
  - Sistema/motor/generators/componentes/assinatura.py
  - Sistema/motor/generators/componentes/comunicado.py
  - Sistema/motor/generators/componentes/calculo.py
  - Sistema/motor/generators/componentes.py
autonomous: true
requirements: [NFR-01, NFR-02]

must_haves:
  truths:
    - "O pacote componentes/ existe com 6 módulos temáticos + __init__.py"
    - "Todos os imports existentes de 'from generators.componentes import build_X' continuam funcionando sem alteração"
    - "O arquivo componentes.py original é convertido em shim que re-importa tudo do pacote"
    - "Nenhuma função pública é perdida na migração (12 funções + 7 internas preservadas)"
    - "O sistema gera um DOCX completo após a refatoração (smoke test passa)"
  artifacts:
    - path: "Sistema/motor/generators/componentes/__init__.py"
      provides: "Shim de compatibilidade — re-exporta todas as funções públicas do pacote"
      exports: ["build_titulo", "build_identificacao", "build_destinatario", "build_dados_carimbo", "build_corpo", "build_conclusao_e_docs", "build_conclusao_simples", "build_assinatura", "build_comunicado_pendencia", "build_partes_envolvidas", "build_historico_cronologico", "build_memoria_calculo"]
    - path: "Sistema/motor/generators/componentes/tabelas.py"
      provides: "Funções de tabelas de metadados do documento"
    - path: "Sistema/motor/generators/componentes/corpo.py"
      provides: "Função build_corpo e helpers de corpo narrativo"
    - path: "Sistema/motor/generators/componentes/conclusao.py"
      provides: "Funções build_conclusao_e_docs e build_conclusao_simples"
    - path: "Sistema/motor/generators/componentes/assinatura.py"
      provides: "Função build_assinatura"
    - path: "Sistema/motor/generators/componentes/comunicado.py"
      provides: "Função build_comunicado_pendencia"
    - path: "Sistema/motor/generators/componentes/calculo.py"
      provides: "Função build_memoria_calculo"
  key_links:
    - from: "geradores_core.py"
      to: "generators.componentes.build_corpo"
      via: "from generators.componentes import build_corpo (via __init__.py shim)"
      pattern: "from generators\\.componentes import"
    - from: "componentes/__init__.py"
      to: "componentes/tabelas.py"
      via: "from generators.componentes.tabelas import *"
      pattern: "from generators\\.componentes\\.tabelas import"
---

<objective>
Quebrar o monolito componentes.py (1048 linhas, 12+ funções misturadas) em um pacote
Python organizado por responsabilidade, mantendo 100% de compatibilidade retroativa
com todos os imports existentes através de um shim __init__.py.

Propósito: componentes.py mistura utilitários de string, renderização de tabelas,
corpo narrativo, conclusão, assinatura, comunicado e cálculo em um arquivo de 1048
linhas. Isso dificulta manutenção, revisão de código e localização de bugs. A quebra
em módulos temáticos permite evolução independente de cada área sem risco de regressão.

Estratégia de compatibilidade: NUNCA alterar os imports em geradores_core.py,
compilador_livre.py ou outros consumidores. O shim __init__.py faz com que
`from generators.componentes import build_corpo` continue funcionando exatamente
como antes — só que agora build_corpo vive em componentes/corpo.py.

Output:
- Sistema/motor/generators/componentes/ (diretório — NOVO)
- Sistema/motor/generators/componentes/__init__.py (shim — NOVO)
- Sistema/motor/generators/componentes/tabelas.py (NOVO)
- Sistema/motor/generators/componentes/corpo.py (NOVO)
- Sistema/motor/generators/componentes/conclusao.py (NOVO)
- Sistema/motor/generators/componentes/assinatura.py (NOVO)
- Sistema/motor/generators/componentes/comunicado.py (NOVO)
- Sistema/motor/generators/componentes/calculo.py (NOVO)
- Sistema/motor/generators/componentes.py (MODIFICADO — vira shim secundário)
</objective>

<execution_context>
@C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\.planning\phases\16-refatoracao-gerador-docx\16-RESEARCH.md
</execution_context>

<context>
@C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\.planning\ROADMAP.md

<interfaces>
<!-- Mapeamento completo das funções em componentes.py e seus destinos -->

Funções PÚBLICAS (exportadas por componentes.py) — distribuição nos módulos:

Para componentes/tabelas.py:
  - build_titulo(doc, titulo="PARECER SETOR TÉCNICO - SMOSU")
  - build_identificacao(doc, d)
  - build_destinatario(doc, d)
  - build_dados_carimbo(doc, d)
  - build_partes_envolvidas(doc, d)
  - build_historico_cronologico(doc, d)

Para componentes/corpo.py:
  - build_corpo(doc, d)

Para componentes/conclusao.py:
  - build_conclusao_e_docs(doc, d)
  - build_conclusao_simples(doc, d)

Para componentes/assinatura.py:
  - build_assinatura(doc, d)

Para componentes/comunicado.py:
  - build_comunicado_pendencia(doc, d)

Para componentes/calculo.py:
  - build_memoria_calculo(doc, d)

Funções INTERNAS (helpers) — destino:
  - _data_hoje_extenso() → tabelas.py (usada em build_identificacao e build_assinatura)
  - _parse_numero(texto) → tabelas.py (usada em build_dados_carimbo)
  - _ensure_list(data) → corpo.py (usada em build_corpo e build_comunicado_pendencia)
  - _apply_cell_fill(cell, hex_color) → tabelas.py (usada em build_dados_carimbo)
  - _set_cell_width(cell, twips) → tabelas.py
  - _build_conclusao_bloco(doc, d) → conclusao.py (helper privado de build_conclusao_e_docs)
  - _box_colorido(doc, fill, borda, borda_sz) → comunicado.py (usada em comunicado e calculo)

Imports que TODOS os módulos vão precisar (copiar de componentes.py):
```python
from core.config import (
    FONT_TITULO, SZ_CORPO, SZ_TABELA,
    COR_LABEL_FONT, COR_CINZA_TEXTO,
    PAR_AFTER, LINE_SPC, ASSINANTE, CIDADE, INDENT_PADRAO,
    # ... etc conforme necessidade de cada módulo
)
from generators.formatacao import (
    set_spacing, set_font, add_run, add_para, rich_segments,
    apply_table_borders, apply_label_cell, apply_value_cell,
    set_cell_margins, add_separator, add_section_heading,
)
```

Nota: _box_colorido é usada tanto em comunicado.py quanto em calculo.py.
Estratégia: definir _box_colorido em comunicado.py e importar em calculo.py.
OU: definir em tabelas.py (que é importado por todos) como helper compartilhado.
Recomendação: definir _box_colorido em componentes/utils.py (NÃO listado originalmente)
ou simplesmente em tabelas.py. A decisão é do executor — DOCUMENTAR a escolha no SUMMARY.

Imports externos que precisam de sys.path insert (padrão já estabelecido no projeto):
```python
import sys as _sys
import os as _os
_MOTOR_DIR = _os.path.dirname(_os.path.dirname(_os.path.abspath(__file__)))
if _MOTOR_DIR not in _sys.path:
    _sys.path.insert(0, _MOTOR_DIR)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar pacote componentes/ com os 6 módulos temáticos</name>
  <files>
    Sistema/motor/generators/componentes/__init__.py
    Sistema/motor/generators/componentes/tabelas.py
    Sistema/motor/generators/componentes/corpo.py
    Sistema/motor/generators/componentes/conclusao.py
    Sistema/motor/generators/componentes/assinatura.py
    Sistema/motor/generators/componentes/comunicado.py
    Sistema/motor/generators/componentes/calculo.py
  </files>

  <read_first>
    - Sistema/motor/generators/componentes.py (ler COMPLETO — é a fonte de todo o código a mover)
    - Sistema/motor/generators/geradores_core.py (linhas 1-36 — ver exatamente quais funções são importadas)
    - Sistema/motor/core/config.py (ver todas as constantes usadas em componentes.py)
    - Sistema/motor/generators/formatacao.py (ver quais helpers são importados em componentes.py)
  </read_first>

  <action>
PASSO 1 — Criar o diretório do pacote:
Criar o diretório Sistema/motor/generators/componentes/ (se não existir).

PASSO 2 — Identificar e separar o código:

Ler componentes.py na íntegra. Para cada seção separada pelos comentários
`# ═══════════ ... ═══════════`, identificar a qual módulo pertence conforme o mapeamento
na seção <interfaces> acima.

PASSO 3 — Criar cada módulo temático:

Para CADA arquivo do pacote:
a) Adicionar o bloco de sys.path no topo (padrão do projeto):
   ```python
   import sys as _sys, os as _os
   _MOTOR_DIR = _os.path.dirname(_os.path.dirname(_os.path.abspath(__file__)))
   if _MOTOR_DIR not in _sys.path:
       _sys.path.insert(0, _MOTOR_DIR)
   ```

b) Copiar apenas os imports de config.py e formatacao.py que o módulo efetivamente usa
   (não copiar todos — só o necessário para as funções que vivem naquele arquivo).

c) Copiar as funções indicadas no mapeamento (exatamente o código de componentes.py,
   sem alterar assinaturas, lógica ou comportamento).

d) Para funções que dependem de outras funções do mesmo arquivo original:
   - Se a dependência vai para o MESMO módulo novo → sem mudança (mesmo arquivo)
   - Se a dependência vai para OUTRO módulo → importar do módulo correspondente

TRATAMENTO ESPECIAL para _box_colorido:
_box_colorido é usada por build_comunicado_pendencia (→ comunicado.py) E
por build_memoria_calculo (→ calculo.py). Colocar _box_colorido em comunicado.py
e, em calculo.py, adicionar import:
```python
from generators.componentes.comunicado import _box_colorido
```

TRATAMENTO ESPECIAL para _data_hoje_extenso:
Usada em build_assinatura (→ assinatura.py) e possivelmente em outras funções.
Colocar em tabelas.py. Em assinatura.py, importar:
```python
from generators.componentes.tabelas import _data_hoje_extenso
```

PASSO 4 — Criar __init__.py como shim de re-exportação:

```python
"""
Pacote componentes — re-exporta todas as funções públicas para compatibilidade retroativa.

Consumidores existentes (geradores_core.py, compilador_livre.py, etc.) que usam:
    from generators.componentes import build_corpo
continuam funcionando sem nenhuma alteração.
"""
from generators.componentes.tabelas import (
    build_titulo,
    build_identificacao,
    build_destinatario,
    build_dados_carimbo,
    build_partes_envolvidas,
    build_historico_cronologico,
)
from generators.componentes.corpo import build_corpo
from generators.componentes.conclusao import (
    build_conclusao_e_docs,
    build_conclusao_simples,
)
from generators.componentes.assinatura import build_assinatura
from generators.componentes.comunicado import build_comunicado_pendencia
from generators.componentes.calculo import build_memoria_calculo

__all__ = [
    "build_titulo", "build_identificacao", "build_destinatario",
    "build_dados_carimbo", "build_corpo",
    "build_conclusao_e_docs", "build_conclusao_simples", "build_assinatura",
    "build_comunicado_pendencia",
    "build_partes_envolvidas", "build_historico_cronologico",
    "build_memoria_calculo",
]
```

ATENÇÃO: O __init__.py do pacote é em componentes/__init__.py (dentro da pasta).
O arquivo componentes.py original (na pasta generators/) será tratado na Tarefa 2.
  </action>

  <verify>
    <automated>
      cd "C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\Sistema\motor" && python -c "
import sys
sys.path.insert(0, '.')

# Verificar que o pacote carrega e exporta todas as funcoes
from generators.componentes import (
    build_titulo, build_identificacao, build_destinatario,
    build_dados_carimbo, build_corpo,
    build_conclusao_e_docs, build_conclusao_simples, build_assinatura,
    build_comunicado_pendencia,
    build_partes_envolvidas, build_historico_cronologico,
    build_memoria_calculo,
)
print('Todas as 12 funcoes publicas importadas com sucesso')

# Verificar que sao callable
funcoes = [
    build_titulo, build_identificacao, build_destinatario,
    build_dados_carimbo, build_corpo,
    build_conclusao_e_docs, build_conclusao_simples, build_assinatura,
    build_comunicado_pendencia,
    build_partes_envolvidas, build_historico_cronologico,
    build_memoria_calculo,
]
for f in funcoes:
    assert callable(f), f'FALHA: {f} nao e callable'

print('Todas as funcoes sao callable — OK')
print('PACOTE COMPONENTES CRIADO COM SUCESSO')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - Diretório Sistema/motor/generators/componentes/ existe
    - Sistema/motor/generators/componentes/__init__.py existe e contém os 12 imports públicos
    - Sistema/motor/generators/componentes/tabelas.py existe e contém def build_titulo
    - Sistema/motor/generators/componentes/corpo.py existe e contém def build_corpo
    - Sistema/motor/generators/componentes/conclusao.py existe e contém def build_conclusao_e_docs
    - Sistema/motor/generators/componentes/assinatura.py existe e contém def build_assinatura
    - Sistema/motor/generators/componentes/comunicado.py existe e contém def build_comunicado_pendencia
    - Sistema/motor/generators/componentes/calculo.py existe e contém def build_memoria_calculo
    - O comando de verificação acima retorna "PACOTE COMPONENTES CRIADO COM SUCESSO" sem erros
  </acceptance_criteria>

  <done>Pacote componentes/ criado com 6 módulos + __init__.py; todas as 12 funções públicas importáveis</done>
</task>

<task type="auto">
  <name>Tarefa 2: Converter componentes.py em shim e validar compatibilidade total</name>
  <files>
    Sistema/motor/generators/componentes.py
  </files>

  <read_first>
    - Sistema/motor/generators/componentes.py (estado atual — antes de modificar)
    - Sistema/motor/generators/componentes/__init__.py (recém-criado na Tarefa 1)
    - Sistema/motor/generators/geradores_core.py (ver imports de componentes para confirmar)
    - Sistema/motor/generators/compilador_livre.py (ver se também importa de componentes)
  </read_first>

  <action>
PASSO 1 — Verificar todos os arquivos que importam de componentes.py:

Fazer grep para confirmar quais arquivos usam:
  `from generators.componentes import`

Esperados: geradores_core.py, compilador_livre.py (possivelmente outros).

PASSO 2 — Converter componentes.py em shim de compatibilidade:

Substituir TODO o conteúdo de componentes.py pelo seguinte shim:

```python
"""
DEPRECATED — Shim de compatibilidade (Fase 16: Refatoração do Gerador DOCX).

Este arquivo foi convertido em um shim de re-exportação. O código real agora
vive no pacote generators/componentes/ (módulos temáticos).

Imports existentes como:
    from generators.componentes import build_corpo
continuam funcionando normalmente através deste shim.

Para novos desenvolvimentos, importe diretamente do módulo temático:
    from generators.componentes.corpo import build_corpo
"""

# Re-exporta tudo do pacote componentes/ para compatibilidade retroativa.
# Qualquer import de 'from generators.componentes import X' continua funcionando.
from generators.componentes import (
    build_titulo,
    build_identificacao,
    build_destinatario,
    build_dados_carimbo,
    build_corpo,
    build_conclusao_e_docs,
    build_conclusao_simples,
    build_assinatura,
    build_comunicado_pendencia,
    build_partes_envolvidas,
    build_historico_cronologico,
    build_memoria_calculo,
)

__all__ = [
    "build_titulo", "build_identificacao", "build_destinatario",
    "build_dados_carimbo", "build_corpo",
    "build_conclusao_e_docs", "build_conclusao_simples", "build_assinatura",
    "build_comunicado_pendencia",
    "build_partes_envolvidas", "build_historico_cronologico",
    "build_memoria_calculo",
]
```

PASSO 3 — NÃO alterar geradores_core.py nem compilador_livre.py.
O shim garante que os imports existentes funcionem sem mudança.

ATENÇÃO: Existe um risco de import circular se componentes.py (shim) importar de
generators.componentes (pacote), e o pacote __init__.py importar de ... etc.
Verificar: o pacote __init__.py importa de generators.componentes.tabelas etc. (módulos
do pacote), NÃO de generators.componentes (o shim). Portanto não há circular.
Mas se o executor encontrar ImportError circular, a solução é alterar o shim para
importar diretamente dos submódulos:
```python
from generators.componentes.tabelas import build_titulo, build_identificacao, ...
from generators.componentes.corpo import build_corpo
# etc.
```
  </action>

  <verify>
    <automated>
      cd "C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\Sistema\motor" && python -c "
import sys
sys.path.insert(0, '.')

# Teste A: import via shim (caminho legado) funciona
from generators.componentes import build_corpo, build_titulo, build_assinatura
from generators.componentes import build_conclusao_e_docs, build_conclusao_simples
from generators.componentes import build_dados_carimbo, build_identificacao
from generators.componentes import build_destinatario, build_comunicado_pendencia
from generators.componentes import build_partes_envolvidas, build_historico_cronologico
from generators.componentes import build_memoria_calculo
print('Todos os imports via shim funcionam')

# Teste B: import direto do submódulo também funciona
from generators.componentes.corpo import build_corpo as bc2
from generators.componentes.tabelas import build_titulo as bt2
assert bc2 is build_corpo, 'FALHA: funcoes do shim e do submodulo devem ser identicas'
assert bt2 is build_titulo, 'FALHA: funcoes do shim e do submodulo devem ser identicas'
print('Imports diretos dos submodulos funcionam e sao identicos ao shim')

# Teste C: geradores_core carrega sem erro (usa imports de componentes)
import generators.geradores_core as gc
print('geradores_core carregado com sucesso')

# Teste D: verificar que componentes.py tem menos de 50 linhas (é um shim, não o monolito)
with open('generators/componentes.py', encoding='utf-8') as f:
    linhas = f.readlines()
assert len(linhas) < 60, f'FALHA: componentes.py tem {len(linhas)} linhas — esperado shim pequeno'
print(f'componentes.py tem {len(linhas)} linhas — confirma que e um shim')

print('SHIM E COMPATIBILIDADE VERIFICADOS COM SUCESSO')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "def build_" "Sistema/motor/generators/componentes.py"` retorna 0 (sem definições de função — é só shim)
    - O tamanho de componentes.py é menor que 60 linhas (shim, não monolito)
    - `grep -c "from generators.componentes import" "Sistema/motor/generators/geradores_core.py"` retorna 1 (import original intacto)
    - O comando de verificação acima retorna "SHIM E COMPATIBILIDADE VERIFICADOS COM SUCESSO" sem erros
    - `python -c "import generators.geradores_core"` no diretório Sistema/motor retorna exit code 0
  </acceptance_criteria>

  <done>componentes.py convertido em shim; todos os imports legados funcionam; geradores_core carrega sem erro</done>
</task>

</tasks>

<threat_model>
## Limites de Confiança

| Limite | Descrição |
|--------|-----------|
| componentes.py → componentes/__init__.py | Risco de import circular se não gerenciado |

## Registro de Ameaças STRIDE

| ID | Categoria | Componente | Disposição | Plano de Mitigação |
|----|-----------|------------|------------|---------------------|
| T-16-07 | Denial of Service | import circular | mitigar | Shim importa de submódulos diretamente (não do __init__); instrução de fallback documentada na ação |
| T-16-08 | Tampering | funções públicas migradas | mitigar | Verificação automatizada confirma identidade (is) entre shim e submódulo |
</threat_model>

<verification>
Smoke test de geração completa após refatoração:

```
cd "C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\Sistema\motor"
python -c "
import json, os
from generators.geradores_core import gerar

dados = {
    'tipo_relatorio': 'alvara_aprovacao',
    'numero_processo': '9999/2026',
    'requerente': 'Teste Refatoracao',
    'logradouro': 'Rua Teste, 1',
    'bairro': 'Centro',
    'profissional_nome': 'Eng. Teste',
    'art_rrt': 'ART-999',
    'zona_uso': 'ZR1',
    'area_total_construida': '100m2',
    'conclusao': 'FAVORÁVEL à aprovação do projeto.',
}
saida = 'Saida/_Testes/teste_refatoracao_componentes.docx'
os.makedirs(os.path.dirname(saida), exist_ok=True)
gerar(dados, saida)
assert os.path.exists(saida), 'DOCX nao gerado'
print('SMOKE TEST PASSOU — refatoração de componentes nao quebrou a geração')
"
```
</verification>

<success_criteria>
- Pacote componentes/ com 7 arquivos (6 módulos + __init__.py)
- componentes.py convertido em shim com menos de 60 linhas
- Todos os 12 imports públicos acessíveis via `from generators.componentes import X`
- geradores_core.py e compilador_livre.py NÃO foram modificados
- Smoke test de geração de DOCX passa
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-refatoracao-gerador-docx/16-03-SUMMARY.md` com:
- Lista dos 7 arquivos criados no pacote
- Como _box_colorido e _data_hoje_extenso foram distribuídos (decisão do executor)
- Resultado do smoke test de geração
- Qualquer import circular encontrado e como foi resolvido
</output>
