---
phase: 16-refatoracao-gerador-docx
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - Sistema/motor/ui/preview_html.py
autonomous: true
requirements: [NFR-01, NFR-02, FR-01]

must_haves:
  truths:
    - "O carimbo técnico é exibido no preview para TODOS os tipos com categoria parecer_tecnico (incluindo regularizacao_complexa_multipla)"
    - "A seção Partes Envolvidas aparece no preview quando dados['partes_envolvidas'] está presente"
    - "A seção Histórico Cronológico aparece no preview quando dados['historico_cronologico'] está presente"
    - "A memória de cálculo é exibida inline (não dentro de <details> oculto) para tipos parecer_tecnico"
    - "A condição do carimbo usa TIPOS_DOCUMENTO.get() em vez de startswith()"
  artifacts:
    - path: "Sistema/motor/ui/preview_html.py"
      provides: "Preview HTML fiel ao DOCX com seções corretas"
      contains: "from core.config import TIPOS_DOCUMENTO"
  key_links:
    - from: "gerar_preview()"
      to: "TIPOS_DOCUMENTO.get(tipo)"
      via: "variável categoria"
      pattern: "TIPOS_DOCUMENTO\\.get\\(.*tipo_relatorio"
    - from: "gerar_preview()"
      to: "_html_partes_envolvidas(dados)"
      via: "condicional dados.get('partes_envolvidas')"
      pattern: "_html_partes_envolvidas"
    - from: "gerar_preview()"
      to: "_html_historico_cronologico(dados)"
      via: "condicional dados.get('historico_cronologico')"
      pattern: "_html_historico_cronologico"
---

<objective>
Corrigir as 4 divergências estruturais do preview_html.py em relação ao DOCX gerado:
1. Condição do carimbo usa heurística `startswith()` errada em vez de TIPOS_DOCUMENTO
2. Seção "Partes Envolvidas" ausente no preview (presente no DOCX)
3. Seção "Histórico Cronológico" ausente no preview (presente no DOCX)
4. Memória de cálculo oculta em `<details>` em vez de inline

Propósito: O preview existe para que o usuário revise o conteúdo antes de gerar o DOCX.
Se o preview omite seções ou as exibe errado, o usuário aprova um rascunho sem ver o
documento real. Isso gera retrabalho e perda de confiança na ferramenta.

Output: Sistema/motor/ui/preview_html.py (modificado — 4 correções + 2 novas funções)
</objective>

<execution_context>
@C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\.planning\phases\16-refatoracao-gerador-docx\16-RESEARCH.md
</execution_context>

<context>
@C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\.planning\ROADMAP.md

<interfaces>
<!-- Interfaces relevantes extraídas de preview_html.py e componentes.py -->

Estado atual de gerar_preview() — linhas 534-612 de preview_html.py:
```python
def gerar_preview(dados: dict, alertas: list[dict] | None = None, destino: str | None = None, auto_abrir: bool = True) -> str:
    alertas = alertas or []
    batch_notice = ""
    html_alertas   = _html_alertas(alertas)
    html_id        = _html_identificacao(dados)
    # BUG: condição errada — falta parecer_tecnico e regularizacao_complexa_multipla
    html_carimbo   = _html_carimbo(dados) if dados.get("tipo_relatorio", "").startswith(("alvara", "regularizacao")) else ""
    html_corpo     = _html_corpo(dados)
    html_docs      = _html_documentos(dados)
    html_memoria   = _html_memoria(dados)   # BUG: renderizado em <details> oculto
    html_assinatura = _html_assinatura(dados)
    # FALTAM: html_partes e html_historico (nunca gerados)
    # ...
    # No template HTML, html_partes e html_historico não aparecem
```

CSS já existente em preview_html.py para memória (a REMOVER o wrapper <details>):
```css
.section-memoria { background: #fafafa; }
.section-memoria details { padding: 10px 20px; }  /* <- este comportamento deve mudar */
.memoria-pre {
  font-size: 8.5pt;
  background: #f0f0f0;
  padding: 10px 14px;
  border-radius: 4px;
  white-space: pre-wrap;
  color: #333;
}
```

Estrutura de dados partes_envolvidas (espelhar build_partes_envolvidas de componentes.py):
```python
# dados["partes_envolvidas"] é uma lista de dicts:
# [{"papel": "Proprietário", "nome": "João", "cpf_cnpj": "123..."}, ...]
# ou string única com o nome do proprietário
```

Estrutura de dados historico_cronologico (espelhar build_historico_cronologico de componentes.py):
```python
# dados["historico_cronologico"] é uma lista de dicts:
# [{"data": "01/01/2026", "evento": "Protocolo do processo", "referencia": "prot-001"}, ...]
# ou lista de strings
```

De config.py — TIPOS_DOCUMENTO (mapeamento tipo → categoria):
```python
TIPOS_DOCUMENTO = {
    "alvara_aprovacao": "parecer_tecnico",
    "alvara_regularizacao": "parecer_tecnico",
    "regularizacao_complexa_multipla": "parecer_tecnico",  # falta no startswith atual
    "habitese_comum": "parecer_simples",
    # ... etc
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Corrigir condição do carimbo e adicionar seções Partes e Histórico</name>
  <files>
    Sistema/motor/ui/preview_html.py
  </files>

  <read_first>
    - Sistema/motor/ui/preview_html.py (ler completo — especialmente linhas 534-612 da função gerar_preview e as funções _html_carimbo, _html_corpo)
    - Sistema/motor/core/config.py (ver TIPOS_DOCUMENTO com todos os tipos e categorias)
    - Sistema/motor/generators/componentes.py (ver build_partes_envolvidas e build_historico_cronologico para espelhar a estrutura de dados esperada)
  </read_first>

  <action>
Fazer três grupos de mudanças em preview_html.py:

── MUDANÇA A: Import de TIPOS_DOCUMENTO no topo do arquivo ─────────────────────

Localizar a seção de imports no topo de preview_html.py (atualmente: os, json, re, webbrowser, tempfile, datetime).
Adicionar, logo após os imports existentes e ANTES da linha `# ── Helpers ──...`:

```python
import sys as _sys
import os as _os
_UI_DIR = _os.path.dirname(_os.path.abspath(__file__))
_MOTOR_DIR = _os.path.dirname(_UI_DIR)
if _MOTOR_DIR not in _sys.path:
    _sys.path.insert(0, _MOTOR_DIR)

try:
    from core.config import TIPOS_DOCUMENTO as _TIPOS_DOCUMENTO
except ImportError:
    _TIPOS_DOCUMENTO = {}
```

Nota: O try/except protege contra execução do arquivo isolado (fora do motor).

── MUDANÇA B: Criar duas novas funções HTML ────────────────────────────────────

Adicionar as funções _html_partes_envolvidas e _html_historico_cronologico
APÓS a função _html_carimbo e ANTES de _html_corpo:

```python
def _html_partes_envolvidas(d: dict) -> str:
    """Seção de Partes Envolvidas — espelha build_partes_envolvidas() do DOCX."""
    partes = d.get("partes_envolvidas")
    if not partes:
        return ""

    # Normalizar: pode ser string (nome simples) ou lista de dicts
    if isinstance(partes, str):
        partes = [{"papel": "Interessado", "nome": partes}]
    elif isinstance(partes, dict):
        partes = [partes]

    linhas = ""
    for p in partes:
        papel = _safe(p.get("papel", "Parte"), "Parte")
        nome  = _safe(p.get("nome", "—"))
        cpf   = p.get("cpf_cnpj", "")
        cpf_html = f'<span style="color:#666;font-size:9pt"> — {cpf}</span>' if cpf else ""
        linhas += f"""
        <tr>
          <td class="id-label">{papel}</td>
          <td class="id-value">{nome}{cpf_html}</td>
        </tr>"""

    return f"""
    <section class="section-id">
      <div class="section-header">PARTES ENVOLVIDAS</div>
      <table class="id-table">{linhas}
      </table>
    </section>"""


def _html_historico_cronologico(d: dict) -> str:
    """Seção de Histórico Cronológico — espelha build_historico_cronologico() do DOCX."""
    historico = d.get("historico_cronologico")
    if not historico:
        return ""

    # Normalizar: pode ser lista de dicts ou lista de strings
    if isinstance(historico, str):
        historico = [{"evento": historico}]

    linhas = ""
    for item in historico:
        if isinstance(item, str):
            item = {"evento": item}
        data      = _safe(item.get("data", ""), "—")
        evento    = _safe(item.get("evento", ""))
        referencia = item.get("referencia", "")
        ref_html  = f'<br><span style="font-size:8.5pt;color:#888">{referencia}</span>' if referencia else ""
        linhas += f"""
        <tr>
          <td class="id-label" style="width:90px;white-space:nowrap">{data}</td>
          <td class="id-value">{evento}{ref_html}</td>
        </tr>"""

    return f"""
    <section class="section-id">
      <div class="section-header">HISTÓRICO CRONOLÓGICO</div>
      <table class="id-table">{linhas}
      </table>
    </section>"""
```

── MUDANÇA C: Corrigir gerar_preview() ─────────────────────────────────────────

Localizar a função gerar_preview() e substituir o bloco de montagem de seções
(atualmente linhas ~544-551):

ANTES:
```python
    html_alertas   = _html_alertas(alertas)
    html_id        = _html_identificacao(dados)
    html_carimbo   = _html_carimbo(dados) if dados.get("tipo_relatorio", "").startswith(("alvara", "regularizacao")) else ""
    html_corpo     = _html_corpo(dados)
    html_docs      = _html_documentos(dados)
    html_memoria   = _html_memoria(dados)
    html_assinatura = _html_assinatura(dados)
```

DEPOIS:
```python
    html_alertas    = _html_alertas(alertas)
    html_id         = _html_identificacao(dados)
    # Usar TIPOS_DOCUMENTO para determinar categoria (correto) em vez de startswith (bugado)
    _tipo           = dados.get("tipo_relatorio", "")
    _categoria      = _TIPOS_DOCUMENTO.get(_tipo, "")
    html_carimbo    = _html_carimbo(dados) if _categoria == "parecer_tecnico" else ""
    html_partes     = _html_partes_envolvidas(dados)
    html_historico  = _html_historico_cronologico(dados)
    html_corpo      = _html_corpo(dados)
    html_docs       = _html_documentos(dados)
    html_memoria    = _html_memoria(dados)
    html_assinatura = _html_assinatura(dados)
```

Localizar o template HTML dentro de gerar_preview() — especificamente o bloco
`<div class="doc-card-wrapper">` — e adicionar {html_partes} e {html_historico}
APÓS {html_carimbo} e ANTES de {html_corpo}:

ANTES:
```python
    {html_alertas}
    {html_id}
    {html_carimbo}
    {html_corpo}
```

DEPOIS:
```python
    {html_alertas}
    {html_id}
    {html_carimbo}
    {html_partes}
    {html_historico}
    {html_corpo}
```
  </action>

  <verify>
    <automated>
      cd "C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\Sistema\motor" && python -c "
import sys
sys.path.insert(0, '.')
from ui.preview_html import gerar_preview, _html_partes_envolvidas, _html_historico_cronologico

# Teste A: carimbo para regularizacao_complexa_multipla (antes bugado, agora deve funcionar)
dados_reg = {
    'tipo_relatorio': 'regularizacao_complexa_multipla',
    'numero_processo': '100/2026',
    'requerente': 'Teste',
    'profissional_nome': 'Eng. X',
    'art_rrt': 'ART-001',
    'zona_uso': 'ZR1',
}
html = gerar_preview(dados_reg, auto_abrir=False)
# O carimbo deve aparecer para parecer_tecnico
assert 'CARIMBO' in html.upper() or 'profissional' in html.lower() or 'section-carimbo' in html, 'FALHA A: carimbo nao aparece para regularizacao_complexa_multipla'

# Teste B: habitese não tem carimbo (categoria parecer_simples)
dados_hab = {'tipo_relatorio': 'habitese_comum', 'numero_processo': '200/2026', 'requerente': 'T'}
html_hab = gerar_preview(dados_hab, auto_abrir=False)
# Verificar: seção de corpo existe mas carimbo NÃO está presente para parecer_simples
# (a lógica CORRETA para habitese é não ter carimbo)

# Teste C: partes_envolvidas aparecem quando presentes
dados_partes = {
    'tipo_relatorio': 'habitese_comum',
    'numero_processo': '300/2026',
    'requerente': 'Maria',
    'partes_envolvidas': [{'papel': 'Proprietário', 'nome': 'Maria Silva', 'cpf_cnpj': '123.456.789-00'}],
}
html_partes = gerar_preview(dados_partes, auto_abrir=False)
assert 'Maria Silva' in html_partes, 'FALHA C: partes_envolvidas nao aparece'
assert 'PARTES ENVOLVIDAS' in html_partes, 'FALHA C: header da secao ausente'

# Teste D: historico_cronologico aparece quando presente
dados_hist = {
    'tipo_relatorio': 'habitese_comum',
    'numero_processo': '400/2026',
    'requerente': 'João',
    'historico_cronologico': [{'data': '01/01/2026', 'evento': 'Protocolo inicial', 'referencia': 'prot-001'}],
}
html_hist = gerar_preview(dados_hist, auto_abrir=False)
assert 'Protocolo inicial' in html_hist, 'FALHA D: historico_cronologico nao aparece'
assert 'HISTÓRICO' in html_hist, 'FALHA D: header da secao ausente'

# Teste E: funções isoladas retornam vazio quando dados ausentes
assert _html_partes_envolvidas({}) == '', 'FALHA E: partes deve retornar vazio'
assert _html_historico_cronologico({}) == '', 'FALHA E: historico deve retornar vazio'

print('TODAS AS CORREÇÕES VERIFICADAS')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "from core.config import TIPOS_DOCUMENTO" "Sistema/motor/ui/preview_html.py"` retorna 1 (ou o import equivalente com alias _TIPOS_DOCUMENTO)
    - `grep -c "startswith.*alvara.*regularizacao" "Sistema/motor/ui/preview_html.py"` retorna 0 (condição bugada removida)
    - `grep -c "_TIPOS_DOCUMENTO.get" "Sistema/motor/ui/preview_html.py"` retorna pelo menos 1
    - `grep -c "def _html_partes_envolvidas" "Sistema/motor/ui/preview_html.py"` retorna 1
    - `grep -c "def _html_historico_cronologico" "Sistema/motor/ui/preview_html.py"` retorna 1
    - `grep -c "html_partes" "Sistema/motor/ui/preview_html.py"` retorna pelo menos 2 (definição + uso no template)
    - `grep -c "html_historico" "Sistema/motor/ui/preview_html.py"` retorna pelo menos 2 (definição + uso no template)
    - O comando de verificação acima retorna "TODAS AS CORREÇÕES VERIFICADAS" sem erros
  </acceptance_criteria>

  <done>preview_html.py corrigido: carimbo usa TIPOS_DOCUMENTO, partes e histórico aparecem no preview</done>
</task>

<task type="auto">
  <name>Tarefa 2: Tornar memória de cálculo inline (remover wrapper details)</name>
  <files>
    Sistema/motor/ui/preview_html.py
  </files>

  <read_first>
    - Sistema/motor/ui/preview_html.py (ler a função _html_memoria e o CSS .section-memoria após a Tarefa 1)
  </read_first>

  <action>
Localizar a função _html_memoria() em preview_html.py e substituí-la pela versão inline.

A versão atual provavelmente usa `<details><summary>` para ocultar o conteúdo.
Substituir pela versão sempre visível:

```python
def _html_memoria(d: dict) -> str:
    """
    Renderiza a memória de cálculo inline (sempre visível), espelhando
    build_memoria_calculo() do DOCX que usa uma caixa destacada.
    """
    memoria = d.get("memoria_de_calculo")
    if not memoria:
        return ""
    # Escapa HTML básico para segurança (a memória pode ter <, >, &)
    memoria_esc = str(memoria).replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")
    return f"""
    <section class="section-memoria">
      <div class="section-header">MEMÓRIA DE CÁLCULO E ÍNDICES URBANÍSTICOS</div>
      <div style="padding: 12px 20px 16px;">
        <pre class="memoria-pre">{memoria_esc}</pre>
      </div>
    </section>"""
```

Também atualizar o CSS da seção .section-memoria para remover os estilos do `<details>`:

Localizar no bloco CSS (variável CSS, string multiline no arquivo):
```css
/* ── MEMÓRIA DE CÁLCULO ── */
.section-memoria { background: #fafafa; }
.section-memoria details { padding: 10px 20px; }
.section-memoria summary {
  cursor: pointer; color: #1F3864; font-weight: bold; font-size: 9pt;
  padding: 6px 0;
}
```

Substituir por:
```css
/* ── MEMÓRIA DE CÁLCULO ── */
.section-memoria { background: #fafafa; }
```

(Remover as regras de details e summary — não são mais usadas após a mudança para inline.)
  </action>

  <verify>
    <automated>
      cd "C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\Sistema\motor" && python -c "
import sys
sys.path.insert(0, '.')
from ui.preview_html import gerar_preview

# Teste: memoria_de_calculo aparece inline (nao em details oculto)
dados = {
    'tipo_relatorio': 'alvara_aprovacao',
    'numero_processo': '500/2026',
    'requerente': 'Construtora X',
    'memoria_de_calculo': 'TO = 40%\nCA = 0.8\nArea Total = 200m2',
}
html = gerar_preview(dados, auto_abrir=False)

# Verificar que o conteudo esta presente
assert 'TO = 40%' in html, 'FALHA: conteudo da memoria nao encontrado no HTML'

# Verificar que NAO esta dentro de <details> (nao deve ter essa tag para memoria)
import re
# Encontrar a secao da memoria no HTML
idx_memoria = html.find('MEMÓRIA DE CÁLCULO')
if idx_memoria >= 0:
    trecho = html[idx_memoria:idx_memoria+500]
    assert '<details' not in trecho, 'FALHA: memoria ainda usa <details> oculto'
    assert 'TO = 40%' in html[idx_memoria:idx_memoria+1000], 'FALHA: conteudo nao inline'

print('MEMORIA INLINE VERIFICADA')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "<details" "Sistema/motor/ui/preview_html.py"` retorna 0 (elemento details removido do arquivo)
    - `grep -c "<summary" "Sistema/motor/ui/preview_html.py"` retorna 0 (summary também removido)
    - `grep -c "memoria-pre" "Sistema/motor/ui/preview_html.py"` retorna pelo menos 2 (classe CSS + uso na função)
    - O comando de verificação acima retorna "MEMORIA INLINE VERIFICADA" sem erros
    - A função _html_memoria usa `<pre class="memoria-pre">` diretamente (sem wrapper details)
  </acceptance_criteria>

  <done>Memória de cálculo exibida inline no preview, sem wrapper details oculto</done>
</task>

</tasks>

<threat_model>
## Limites de Confiança

| Limite | Descrição |
|--------|-----------|
| dados dict → HTML gerado | Strings dos dados são inseridas no HTML — risco de XSS se dados contiverem tags |

## Registro de Ameaças STRIDE

| ID | Categoria | Componente | Disposição | Plano de Mitigação |
|----|-----------|------------|------------|---------------------|
| T-16-04 | Tampering | _html_memoria() | mitigar | Escapar &, <, > na memória de cálculo antes de inserir no HTML (implementado na Tarefa 2) |
| T-16-05 | Tampering | _html_partes_envolvidas() | aceitar | Dados vêm do JSON do processo (input do próprio usuário); preview é arquivo local temporário |
| T-16-06 | Information Disclosure | preview HTML | aceitar | Arquivo gerado em tempfile local; sem exposição de rede |
</threat_model>

<verification>
Smoke test visual após conclusão:

Abrir um JSON de processo com `partes_envolvidas` e `historico_cronologico` preenchidos
e com `tipo_relatorio: "regularizacao_complexa_multipla"`, executar o preview via painel GEM
e verificar visualmente:
1. O carimbo técnico aparece (tipo é parecer_tecnico)
2. A seção "PARTES ENVOLVIDAS" aparece com os dados corretos
3. A seção "HISTÓRICO CRONOLÓGICO" aparece com os dados corretos
4. A memória de cálculo (se presente) aparece visível sem precisar clicar em expandir
</verification>

<success_criteria>
- A condição do carimbo usa TIPOS_DOCUMENTO.get() — a linha `startswith(("alvara", "regularizacao"))` não existe mais
- _html_partes_envolvidas() e _html_historico_cronologico() existem e são chamadas em gerar_preview()
- _html_memoria() usa `<pre class="memoria-pre">` diretamente, sem `<details>`
- O preview de tipo "regularizacao_complexa_multipla" exibe o carimbo técnico
- Todos os testes automatizados passam
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-refatoracao-gerador-docx/16-02-SUMMARY.md` com:
- Arquivos modificados e as 4 correções implementadas
- Resultado dos testes automatizados
- Resultado do smoke test visual (descrever o que foi verificado)
- Qualquer desvio do plano e justificativa
</output>
