---
phase: 16-refatoracao-gerador-docx
plan: 04
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03]
files_modified:
  - Sistema/motor/templates/alvara_aprovacao.json
  - Sistema/motor/templates/alvara_regularizacao.json
  - Sistema/motor/templates/habitese_comum.json
  - Sistema/motor/templates/alvara_mcmv.json
  - Sistema/motor/templates/alvara_construcao_comercial.json
autonomous: true
requirements: [NFR-01, NFR-02, FR-01]

must_haves:
  truths:
    - "Nenhum template JSON contém placeholders descritivos no estilo [campo_ex_valor] nos campos modelo_*"
    - "Os modelos_considerandos dos templates retêm apenas placeholders reais ([numero_processo], [requerente], etc.) e condicionais [SE campo]"
    - "A geração de um documento alvara_aprovacao com JSON mínimo não produz texto com _ex_ no DOCX final"
    - "A geração de um habitese_comum com JSON mínimo sem considerandos usa o modelo_considerandos e produz texto narrativo real"
    - "O pipeline completo (JSON → enricher → DOCX) funciona end-to-end para os 5 tipos de template limpos"
  artifacts:
    - path: "Sistema/motor/templates/alvara_aprovacao.json"
      provides: "Template limpo sem placeholders descritivos"
    - path: "Sistema/motor/templates/alvara_regularizacao.json"
      provides: "Template limpo"
    - path: "Sistema/motor/templates/habitese_comum.json"
      provides: "Template limpo — modelo_considerandos com [SE campo] tratável"
    - path: "Sistema/motor/templates/alvara_mcmv.json"
      provides: "Template limpo"
    - path: "Sistema/motor/templates/alvara_construcao_comercial.json"
      provides: "Template limpo"
  key_links:
    - from: "enricher._interpolar()"
      to: "template.modelo_abertura"
      via: "regex [campo] → dados[campo]"
      pattern: "\\[([^\\]]+)\\]"
---

<objective>
Limpar os placeholders descritivos dos templates JSON prioritários (os 5 mais usados)
e executar validação end-to-end do pipeline completo após todas as refatorações das
Fases 16-01, 16-02 e 16-03.

Propósito: O enricher.py (Plano 01) sabe detectar e remover placeholders descritivos
(_ex_ ou >40 chars), mas é melhor higienizar os templates diretamente para evitar
conteúdo residual e tornar os modelos prontos para humanos lerem e editarem.
A validação end-to-end confirma que todos os planos anteriores funcionam de forma
integrada sem regressão.

Output:
- 5 templates JSON limpos (sem [campo_ex_...])
- Relatório de smoke tests de geração para cada tipo
</objective>

<execution_context>
@C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\.planning\phases\16-refatoracao-gerador-docx\16-RESEARCH.md
</execution_context>

<context>
@C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\.planning\ROADMAP.md

<interfaces>
<!-- Estrutura dos templates JSON a limpar -->

Exemplo de placeholder DESCRITIVO a remover (instrução para o Gemini, não campo real):
  [tipo_projeto_ex_residencial_unifamiliar]  ← contém "_ex_" → REMOVER
  [endereco_completo_ex_rua_flores_123]      ← contém "_ex_" → REMOVER
  [data_ex_dd_mm_aaaa]                       ← contém "_ex_" → REMOVER

Exemplo de placeholder REAL a MANTER (campo que vem do JSON do processo):
  [numero_processo]     ← campo real do JSON
  [requerente]          ← campo real do JSON
  [logradouro]          ← campo real do JSON
  [numero_alvara]       ← campo real do JSON
  [area_total_construida] ← campo real do JSON

Exemplo de condicional a MANTER:
  [SE data_complemento] conforme complemento...  ← lógica condicional válida
  [OBRIGATÓRIO] o imóvel possui...              ← remover apenas o prefixo [OBRIGATÓRIO]

Campos comuns do JSON de processo (MANTER nos modelos):
  numero_processo, requerente, logradouro, bairro, numero, complemento,
  data_processo, profissional_nome, art_rrt, zona_uso, tipo_relatorio,
  area_total_construida, area_terreno, numero_alvara, matricula_sri,
  data_alvara, data_complemento, tipo_uso, carga_incendio, numero_pavimentos,
  uso_pretendido, proprietario_nome, interessado, inscricao_municipal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Auditar e limpar templates prioritários</name>
  <files>
    Sistema/motor/templates/alvara_aprovacao.json
    Sistema/motor/templates/alvara_regularizacao.json
    Sistema/motor/templates/habitese_comum.json
    Sistema/motor/templates/alvara_mcmv.json
    Sistema/motor/templates/alvara_construcao_comercial.json
  </files>

  <read_first>
    - Sistema/motor/templates/alvara_aprovacao.json (ler completo)
    - Sistema/motor/templates/alvara_regularizacao.json (ler completo)
    - Sistema/motor/templates/habitese_comum.json (ler completo)
    - Sistema/motor/templates/alvara_mcmv.json (ler completo)
    - Sistema/motor/templates/alvara_construcao_comercial.json (ler completo)
    - Sistema/motor/generators/enricher.py (confirmar quais placeholders serão detectados automaticamente)
  </read_first>

  <action>
Para CADA um dos 5 templates, seguir o processo:

PASSO 1 — Ler o template JSON completo.

PASSO 2 — Identificar placeholders descritivos nos campos modelo_*:
Procurar padrões `[texto_com_ex_no_nome]` ou `[texto_muito_longo_descritivo]`
nos campos: modelo_abertura, modelo_considerandos, modelo_conclusao.

PASSO 3 — Limpar os placeholders descritivos:

REGRA A: Placeholder com "_ex_" → remover completamente o placeholder (deixar string vazia no lugar)
REGRA B: Placeholder com mais de 40 caracteres → idem
REGRA C: Prefixo [OBRIGATÓRIO] nos itens de modelo_considerandos → remover o prefixo [OBRIGATÓRIO], manter o restante do texto
REGRA D: [SE campo] → MANTER (é lógica condicional válida que o enricher trata)
REGRA E: [campo_real] → MANTER (é interpolação real que o enricher substituirá)

EXEMPLOS de transformações esperadas:

Em modelo_abertura de alvara_aprovacao.json:
  ANTES: "Trata-se de requerimento de [tipo_projeto_ex_residencial_unifamiliar] para o imóvel..."
  DEPOIS: "Trata-se de requerimento de aprovação de projeto para o imóvel..."
  (remover o placeholder descritivo e adequar a frase para fazer sentido sem ele)

Em modelo_considerandos de habitese_comum.json:
  ANTES: "[OBRIGATÓRIO] o imóvel possui Alvará de Construção nº [numero_alvara]..."
  DEPOIS: "o imóvel possui Alvará de Construção nº [numero_alvara]..."
  (apenas remover o prefixo [OBRIGATÓRIO])

PASSO 4 — Salvar os JSONs editados mantendo formatação/indentação original.
Usar json.dumps com indent=2 e ensure_ascii=False.

ATENÇÃO: Preservar TODOS os outros campos dos templates (campos_obrigatorios,
documentos_tipicos, titulo_documento, etc.). Modificar SOMENTE os campos modelo_*.
Não alterar a estrutura, não remover campos, não alterar documentos_tipicos.
  </action>

  <verify>
    <automated>
      cd "C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\Sistema\motor" && python -c "
import json, re

templates = [
    'templates/alvara_aprovacao.json',
    'templates/alvara_regularizacao.json',
    'templates/habitese_comum.json',
    'templates/alvara_mcmv.json',
    'templates/alvara_construcao_comercial.json',
]

descritivo_pattern = re.compile(r'\[[^\]]*_ex_[^\]]*\]')

for t in templates:
    with open(t, encoding='utf-8') as f:
        data = json.load(f)

    # Verificar que os modelos nao contem _ex_
    for campo in ['modelo_abertura', 'modelo_conclusao']:
        texto = data.get(campo, '')
        matches = descritivo_pattern.findall(texto)
        assert not matches, f'FALHA em {t} campo {campo}: ainda contém {matches}'

    for item in data.get('modelo_considerandos', []):
        matches = descritivo_pattern.findall(item)
        assert not matches, f'FALHA em {t} modelo_considerandos: ainda contém {matches}'

    # Verificar que o JSON ainda é válido e preservou campos_obrigatorios
    assert 'campos_obrigatorios' in data or True, f'AVISO: {t} nao tem campos_obrigatorios'

    print(f'  OK: {t}')

print('TODOS OS TEMPLATES LIMPOS — sem placeholders descritivos')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - O comando de verificação acima retorna "TODOS OS TEMPLATES LIMPOS — sem placeholders descritivos" sem erros
    - Nenhum dos 5 templates contém a string "_ex_" nos campos modelo_* (verificável com grep)
    - Os 5 arquivos JSON ainda são válidos (python -m json.tool verifica sintaxe)
    - Os campos campos_obrigatorios e documentos_tipicos dos 5 templates estão intactos (não foram alterados)
    - `grep -r "_ex_" "Sistema/motor/templates/alvara_aprovacao.json"` retorna 0 resultados
    - `grep -r "_ex_" "Sistema/motor/templates/habitese_comum.json"` retorna 0 resultados
  </acceptance_criteria>

  <done>5 templates limpos: sem [placeholder_ex_descritivo], modelos narrativos prontos para uso pelo enricher</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Tarefa 2: Validação end-to-end do pipeline completo (Fase 16)</name>

  <what-built>
    O executor automatizou o smoke test de geração de DOCX para os 5 tipos de documento
    com templates limpos, usando JSONs mínimos (sem considerandos fornecidos pelo Gemini)
    para forçar o enricher a usar os modelos do template.

    Script executado automaticamente:
    ```
    cd Sistema/motor
    python -c "
    import json, os, re
    from generators.geradores_core import gerar

    os.makedirs('Saida/_Testes', exist_ok=True)

    casos = [
        {'tipo_relatorio': 'alvara_aprovacao', 'numero_processo': 'T01/2026',
         'requerente': 'Teste 01', 'logradouro': 'Rua A, 1', 'bairro': 'Centro',
         'profissional_nome': 'Eng. X', 'art_rrt': 'ART-001', 'zona_uso': 'ZR1'},
        {'tipo_relatorio': 'alvara_regularizacao', 'numero_processo': 'T02/2026',
         'requerente': 'Teste 02', 'logradouro': 'Rua B, 2', 'bairro': 'Centro',
         'profissional_nome': 'Eng. X', 'art_rrt': 'ART-002', 'zona_uso': 'ZR1'},
        {'tipo_relatorio': 'habitese_comum', 'numero_processo': 'T03/2026',
         'requerente': 'Teste 03', 'logradouro': 'Rua C, 3', 'bairro': 'Centro',
         'profissional_nome': 'Eng. X', 'art_rrt': 'ART-003', 'zona_uso': 'ZR1'},
        {'tipo_relatorio': 'alvara_mcmv', 'numero_processo': 'T04/2026',
         'requerente': 'Teste 04', 'logradouro': 'Rua D, 4', 'bairro': 'Centro',
         'profissional_nome': 'Eng. X', 'art_rrt': 'ART-004', 'zona_uso': 'ZR1'},
        {'tipo_relatorio': 'alvara_construcao_comercial', 'numero_processo': 'T05/2026',
         'requerente': 'Teste 05', 'logradouro': 'Rua E, 5', 'bairro': 'Centro',
         'profissional_nome': 'Eng. X', 'art_rrt': 'ART-005', 'zona_uso': 'ZR1'},
    ]

    resultados = []
    for caso in casos:
        tipo = caso['tipo_relatorio']
        saida = f'Saida/_Testes/fase16_smoke_{tipo}.docx'
        try:
            gerar(caso, saida)
            existe = os.path.exists(saida)
            tamanho = os.path.getsize(saida) if existe else 0
            resultados.append({'tipo': tipo, 'ok': existe and tamanho > 5000, 'tamanho': tamanho})
        except Exception as e:
            resultados.append({'tipo': tipo, 'ok': False, 'erro': str(e)})

    print('Resultados dos smoke tests:')
    todos_ok = True
    for r in resultados:
        status = 'PASSOU' if r.get('ok') else 'FALHOU'
        if not r.get('ok'):
            todos_ok = False
        print(f'  {status}: {r[\"tipo\"]} — {r.get(\"tamanho\", 0)} bytes — {r.get(\"erro\", \"\")}')

    if todos_ok:
        print('TODOS OS SMOKE TESTS PASSARAM')
    else:
        print('SMOKE TESTS COM FALHA — ver acima')
    "
    ```
  </what-built>

  <how-to-verify>
    Execute o script acima no diretório Sistema/motor e depois:

    1. Verifique a saída no terminal — deve mostrar "TODOS OS SMOKE TESTS PASSARAM"

    2. Abra cada um dos 5 arquivos gerados em Saida/_Testes/:
       - fase16_smoke_alvara_aprovacao.docx
       - fase16_smoke_alvara_regularizacao.docx
       - fase16_smoke_habitese_comum.docx
       - fase16_smoke_alvara_mcmv.docx
       - fase16_smoke_alvara_construcao_comercial.docx

    3. Para cada arquivo, verificar visualmente:
       a. O arquivo abre sem erro no Word/LibreOffice
       b. O documento tem conteúdo de texto (não está em branco)
       c. NÃO aparece o texto "[PREENCHER: considerandos]" no corpo
       d. NÃO aparece texto com "_ex_" no meio de uma frase
       e. O cabeçalho institucional (brasão + nome da prefeitura) está presente

    4. Especificamente para habitese_comum:
       - O corpo deve conter considerandos narrativos vindos do modelo_considerandos
       - NÃO deve aparecer "[PREENCHER: considerandos]"
       - Pode aparecer [numero_alvara] se esse campo não foi fornecido no JSON mínimo
         (comportamento correto — enricher mantém placeholder real quando dado ausente)

    5. Se qualquer arquivo falhar, descreva o problema ao retomar.
  </how-to-verify>

  <resume-signal>
    Digite "aprovado" se todos os 5 documentos abriram, têm conteúdo e não mostram
    [PREENCHER: considerandos] no corpo do texto.
    Ou descreva qualquer problema encontrado (ex: "habitese tem [PREENCHER] no corpo",
    "alvara_aprovacao falhou com ImportError").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Limites de Confiança

| Limite | Descrição |
|--------|-----------|
| Templates JSON editados → enricher | Placeholders incorretamente removidos podem produzir frases sem sentido |

## Registro de Ameaças STRIDE

| ID | Categoria | Componente | Disposição | Plano de Mitigação |
|----|-----------|------------|------------|---------------------|
| T-16-09 | Tampering | templates JSON | mitigar | Verificação automatizada confirma ausência de _ex_ e validade do JSON após edição |
| T-16-10 | Denial of Service | smoke test com 5 tipos | aceitar | Smoke test local; sem risco operacional; se falhar, retomar com diagnóstico |
</threat_model>

<verification>
A validação end-to-end é a Tarefa 2 (checkpoint:human-verify).

Verificação adicional de integridade dos templates:

```
cd "C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\Sistema\motor"
python -c "
import json, os

templates_dir = 'templates'
erros = []
for nome in os.listdir(templates_dir):
    if not nome.endswith('.json'):
        continue
    caminho = os.path.join(templates_dir, nome)
    try:
        with open(caminho, encoding='utf-8') as f:
            json.load(f)
    except json.JSONDecodeError as e:
        erros.append(f'{nome}: {e}')

if erros:
    print('JSONs INVÁLIDOS:')
    for e in erros: print(f'  {e}')
else:
    total = len([f for f in os.listdir(templates_dir) if f.endswith('.json')])
    print(f'TODOS OS {total} TEMPLATES JSON SÃO VÁLIDOS')
"
```
</verification>

<success_criteria>
- 5 templates limpos sem placeholders _ex_ nos campos modelo_*
- Verificação automatizada da Tarefa 1 retorna "TODOS OS TEMPLATES LIMPOS"
- Smoke tests de geração: 5 DOCXs gerados com tamanho > 5.000 bytes cada
- Checkpoint humano aprovado: documentos abrem sem erro e têm conteúdo narrativo real
- habitese_comum gerado sem "[PREENCHER: considerandos]" no corpo
- Todos os templates JSON permanecem sintaticamente válidos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-refatoracao-gerador-docx/16-04-SUMMARY.md` com:
- Lista dos 5 templates modificados e principais mudanças em cada um
- Resultado dos smoke tests (tamanho de cada DOCX gerado)
- Resultado do checkpoint de verificação visual
- Status final da Fase 16 (todos os 4 planos concluídos)
- Quaisquer templates adicionais identificados como necessitando limpeza (para fase futura)
</output>
