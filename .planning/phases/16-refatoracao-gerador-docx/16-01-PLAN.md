---
phase: 16-refatoracao-gerador-docx
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Sistema/motor/generators/enricher.py
  - Sistema/motor/generators/geradores_core.py
autonomous: true
requirements: [NFR-01, NFR-02, FR-01]

must_haves:
  truths:
    - "O arquivo enricher.py existe em Sistema/motor/generators/ com a função pública enriquecer_dados(dados, template)"
    - "A função enriquecer_dados preenche paragrafo_abertura, considerandos, conclusao e documentos_emitir SOMENTE quando esses campos estão ausentes ou vazios"
    - "Dados já fornecidos pelo Gemini nunca são sobrescritos pelo enriquecedor"
    - "Placeholders condicionais [SE campo] são tratados: campo presente → texto sem prefixo; campo ausente → item omitido"
    - "Placeholders descritivos (contendo '_ex_' ou com mais de 40 caracteres) são removidos silenciosamente"
    - "geradores_core.py chama enriquecer_dados() após normalizar_dados() e antes de criar o documento"
    - "A geração de um habitese_comum com JSON mínimo (sem considerandos) não produz [PREENCHER: considerandos] no documento"
  artifacts:
    - path: "Sistema/motor/generators/enricher.py"
      provides: "Módulo de enriquecimento de dados com modelos do template"
      exports: ["enriquecer_dados", "_interpolar", "_tratar_condicional"]
    - path: "Sistema/motor/generators/geradores_core.py"
      provides: "Pipeline de geração com enriquecimento integrado"
      contains: "from generators.enricher import enriquecer_dados"
  key_links:
    - from: "geradores_core.gerar()"
      to: "enricher.enriquecer_dados()"
      via: "chamada direta após normalizar_dados()"
      pattern: "enriquecer_dados\\(dados, template\\)"
    - from: "enricher._interpolar()"
      to: "template['modelo_abertura']"
      via: "re.sub com regex \\[([^\\]]+)\\]"
      pattern: "re\\.sub.*\\[\\("
---

<objective>
Criar o módulo enricher.py — o estágio de enriquecimento de dados que interpola os
modelos narrativos dos templates JSON com os dados concretos do processo — e integrá-lo
ao pipeline principal em geradores_core.py.

Propósito: Corrigir o problema central da Fase 16: os templates JSON contêm modelos
narrativos ricos (modelo_abertura, modelo_considerandos, modelo_conclusao) que nunca
são aplicados. Quando o Gemini não produz texto suficiente, o fallback atual insere
[PREENCHER: campo] em documentos oficiais. O enricher elimina este problema fornecendo
conteúdo narrativo real como fallback.

Output:
- Sistema/motor/generators/enricher.py (novo, ~110 linhas)
- Sistema/motor/generators/geradores_core.py (modificado: import + 1 linha de chamada)
</objective>

<execution_context>
@C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\.planning\phases\16-refatoracao-gerador-docx\16-RESEARCH.md
</execution_context>

<context>
@C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\.planning\ROADMAP.md

<interfaces>
<!-- Interfaces relevantes extraídas do código-fonte. O executor NÃO precisa explorar o codebase. -->

De geradores_core.py (linhas 382–459):
```python
# Fluxo atual da função gerar():
def gerar(dados, caminho_saida=None):
    tipo = dados.get("tipo_relatorio")          # linha 340
    # ... achatamento de campos_obrigatorios ... # linhas 348-361
    categoria = TIPOS_DOCUMENTO.get(tipo)        # linha 382
    template = carregar_template(tipo)           # linha 393
    obrigatorios = template.get("campos_obrigatorios", [])  # linha 396
    from generators._aliases import normalizar_dados         # linha 399
    dados = normalizar_dados(dados)                          # linha 400
    # <<< INSERIR AQUI: dados = enriquecer_dados(dados, template) >>>
    # ... validação de campos faltantes (linhas 408-451) ...
    doc = _criar_documento_base()                # linha 454
    gerador_fn = GERADORES[categoria]            # linha 458
    gerador_fn(doc, dados, template)             # linha 459
```

De templates/habitese_comum.json (estrutura real):
```json
{
  "modelo_considerandos": [
    "[SE data_complemento] conforme complemento anexado... a construção confere com o projeto aprovado...",
    "[OBRIGATÓRIO] o imóvel possui Alvará de Construção nº [numero_alvara]...",
    "o requerente apresentou... Matrícula [matricula_sri]..."
  ],
  "modelo_abertura": "Trata-se de requerimento de [tipo_relatorio] para o imóvel situado em [logradouro]...",
  "modelo_conclusao": "Ante o exposto, emito parecer [conclusao_tipo] ao Habite-se..."
}
```

De templates/alvara_aprovacao.json (placeholder descritivo — NÃO é campo real):
```json
{
  "modelo_abertura": "Trata-se de requerimento de [tipo_projeto_ex_residencial_unifamiliar]..."
}
```
Nota: [tipo_projeto_ex_residencial_unifamiliar] contém "_ex_" → placeholder descritivo → deve ser removido.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar enricher.py com interpolação e tratamento condicional</name>
  <files>
    Sistema/motor/generators/enricher.py
  </files>

  <read_first>
    - Sistema/motor/generators/geradores_core.py (ver função carregar_template e o fluxo de gerar())
    - Sistema/motor/templates/habitese_comum.json (ver modelo_considerandos com [SE campo])
    - Sistema/motor/templates/alvara_aprovacao.json (ver placeholder descritivo [tipo_projeto_ex_residencial_unifamiliar])
    - Sistema/motor/core/config.py (ver TEMPLATES_DIR)
  </read_first>

  <behavior>
    - Teste 1: _interpolar("[numero_processo] - [requerente]", {"numero_processo": "3425/2026", "requerente": "João"}) == "3425/2026 - João"
    - Teste 2: _interpolar("[campo_ausente] texto", {}) == "[campo_ausente] texto" (mantém placeholder intacto)
    - Teste 3: _interpolar("[tipo_projeto_ex_residencial_unifamiliar]", {}) == "" (remove descritivo com _ex_)
    - Teste 4: _tratar_condicional("[SE data_complemento] texto do complemento", {"data_complemento": "01/01/2026"}) == "texto do complemento"
    - Teste 5: _tratar_condicional("[SE data_complemento] texto", {}) is None (campo ausente → omite)
    - Teste 6: _tratar_condicional("item normal sem condicional", {}) == "item normal sem condicional"
    - Teste 7: enriquecer_dados com paragrafo_abertura já preenchido pelo Gemini → não altera o valor
    - Teste 8: enriquecer_dados com considerandos vazio → preenche com modelos do template
    - Teste 9: enriquecer_dados com considerandos=["item do Gemini"] → não substitui
  </behavior>

  <action>
Criar o arquivo Sistema/motor/generators/enricher.py com exatamente o seguinte conteúdo:

```python
"""
Enricher — estágio de enriquecimento de dados com modelos do template.

Interpola os campos [campo] dos modelos narrativos (modelo_abertura,
modelo_considerandos, modelo_conclusao, documentos_tipicos) com os
dados concretos do processo. Só preenche campos AUSENTES ou VAZIOS —
jamais sobrescreve o que o Gemini já produziu.
"""

import re


# ═══════════════════════════════════════════════════════════
#  INTERPOLAÇÃO DE PLACEHOLDERS
# ═══════════════════════════════════════════════════════════

def _interpolar(texto: str, dados: dict) -> str:
    """
    Substitui [campo] pelo valor de dados['campo'].
    Regras:
    - Se campo existe em dados → substitui pelo valor
    - Se campo contém '_ex_' ou tem mais de 40 chars → placeholder descritivo → remove
    - Se campo ausente (sem _ex_, curto) → mantém [campo] intacto
    """
    def repl(m):
        chave = m.group(1)
        # Placeholder descritivo (instrução para o Gemini, não campo real)
        if "_ex_" in chave or len(chave) > 40:
            return ""
        val = dados.get(chave, "")
        return str(val) if val else m.group(0)  # mantém se ausente

    return re.sub(r'\[([^\]]+)\]', repl, texto)


def _tratar_condicional(item: str, dados: dict) -> str | None:
    """
    Trata a sintaxe [SE campo] texto...
    - Se campo presente em dados → retorna texto sem o prefixo [SE campo]
    - Se campo ausente → retorna None (item deve ser omitido da lista)
    - Se item não começa com [SE ...] → retorna item sem alteração
    """
    m = re.match(r'^\[SE ([^\]]+)\]\s*(.*)', item, re.DOTALL)
    if m:
        campo = m.group(1).strip()
        resto = m.group(2).strip()
        return resto if dados.get(campo) else None
    return item  # não é condicional


# ═══════════════════════════════════════════════════════════
#  ENRIQUECIMENTO PRINCIPAL
# ═══════════════════════════════════════════════════════════

def enriquecer_dados(dados: dict, template: dict) -> dict:
    """
    Aplica os modelos do template em campos ausentes do dict de dados.
    Preserva 100% do conteúdo que o Gemini já produziu.

    Campos enriquecidos (somente se ausentes/vazios):
    - paragrafo_abertura  ← modelo_abertura
    - considerandos       ← modelo_considerandos (lista, com tratamento [SE campo])
    - conclusao           ← modelo_conclusao
    - documentos_emitir   ← documentos_tipicos
    """
    # ── paragrafo_abertura ─────────────────────────────────────────────────────
    if not dados.get("paragrafo_abertura"):
        modelo = template.get("modelo_abertura", "")
        if modelo:
            interpolado = _interpolar(modelo, dados).strip()
            if interpolado:
                dados["paragrafo_abertura"] = interpolado

    # ── considerandos ──────────────────────────────────────────────────────────
    considerandos_atuais = dados.get("considerandos")
    if not considerandos_atuais or (
        isinstance(considerandos_atuais, list) and len(considerandos_atuais) == 0
    ):
        modelos = template.get("modelo_considerandos", [])
        if modelos:
            resultado = []
            for item in modelos:
                # Remove prefixo [OBRIGATÓRIO] se presente (era instrução para Gemini)
                item_limpo = re.sub(r'^\[OBRIGATÓRIO\]\s*', '', item).strip()
                # Trata condicionais [SE campo]
                tratado = _tratar_condicional(item_limpo, dados)
                if tratado is None:
                    continue  # campo condicional ausente → omite
                # Interpola placeholders reais
                interpolado = _interpolar(tratado, dados).strip()
                if interpolado:
                    resultado.append(interpolado)
            if resultado:
                dados["considerandos"] = resultado

    # ── conclusao ──────────────────────────────────────────────────────────────
    if not dados.get("conclusao"):
        modelo = template.get("modelo_conclusao", "")
        if modelo:
            interpolado = _interpolar(modelo, dados).strip()
            if interpolado:
                dados["conclusao"] = interpolado

    # ── documentos_emitir ──────────────────────────────────────────────────────
    if not dados.get("documentos_emitir"):
        tipicos = template.get("documentos_tipicos", [])
        if tipicos:
            docs = []
            for d in tipicos:
                tipo_interpolado = _interpolar(d.get("tipo", ""), dados).strip()
                obs_raw = d.get("obs", "")
                obs_interpolado = _interpolar(obs_raw, dados).strip() if obs_raw else None
                if tipo_interpolado:
                    docs.append({
                        "tipo": tipo_interpolado,
                        "obs": obs_interpolado if obs_interpolado else None,
                    })
            if docs:
                dados["documentos_emitir"] = docs

    return dados
```

O arquivo deve ter exatamente estas funções: _interpolar, _tratar_condicional, enriquecer_dados.
Não adicionar imports de outros módulos do projeto (apenas re da stdlib).
  </action>

  <verify>
    <automated>
      cd "C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\Sistema\motor" && python -c "
from generators.enricher import enriquecer_dados, _interpolar, _tratar_condicional

# Teste 1: interpolação básica
assert _interpolar('[numero_processo] - [requerente]', {'numero_processo': '3425/2026', 'requerente': 'João'}) == '3425/2026 - João', 'FALHA T1'

# Teste 2: mantém placeholder ausente
assert '[campo_ausente]' in _interpolar('[campo_ausente] texto', {}), 'FALHA T2'

# Teste 3: remove placeholder descritivo
assert _interpolar('[tipo_projeto_ex_residencial_unifamiliar]', {}) == '', 'FALHA T3'

# Teste 4: [SE campo] presente
assert _tratar_condicional('[SE data_complemento] texto ok', {'data_complemento': '01/01/2026'}) == 'texto ok', 'FALHA T4'

# Teste 5: [SE campo] ausente
assert _tratar_condicional('[SE data_complemento] texto', {}) is None, 'FALHA T5'

# Teste 6: item normal sem condicional
assert _tratar_condicional('item normal', {}) == 'item normal', 'FALHA T6'

# Teste 7: não sobrescreve paragrafo_abertura existente
dados = {'paragrafo_abertura': 'Texto do Gemini', 'tipo_relatorio': 'habitese_comum'}
template = {'modelo_abertura': 'Modelo do template [campo]'}
resultado = enriquecer_dados(dados, template)
assert resultado['paragrafo_abertura'] == 'Texto do Gemini', 'FALHA T7'

# Teste 8: preenche considerandos vazio
dados = {'tipo_relatorio': 'habitese_comum'}
template = {'modelo_considerandos': ['item um [numero_processo]', 'item dois']}
resultado = enriquecer_dados(dados, template)
assert isinstance(resultado.get('considerandos'), list) and len(resultado['considerandos']) > 0, 'FALHA T8'

# Teste 9: não sobrescreve considerandos do Gemini
dados = {'considerandos': ['item do Gemini']}
template = {'modelo_considerandos': ['modelo do template']}
resultado = enriquecer_dados(dados, template)
assert resultado['considerandos'] == ['item do Gemini'], 'FALHA T9'

print('TODOS OS TESTES PASSARAM')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - Sistema/motor/generators/enricher.py existe e não está vazio
    - `grep -c "def enriquecer_dados" "Sistema/motor/generators/enricher.py"` retorna 1
    - `grep -c "def _interpolar" "Sistema/motor/generators/enricher.py"` retorna 1
    - `grep -c "def _tratar_condicional" "Sistema/motor/generators/enricher.py"` retorna 1
    - `grep -c "_ex_" "Sistema/motor/generators/enricher.py"` retorna pelo menos 1 (lógica de placeholder descritivo presente)
    - `grep -c "SE " "Sistema/motor/generators/enricher.py"` retorna pelo menos 1 (lógica condicional presente)
    - O comando de verificação automatizada acima retorna "TODOS OS TESTES PASSARAM" sem erros
  </acceptance_criteria>

  <done>enricher.py criado com as três funções públicas, todos os 9 testes passam</done>
</task>

<task type="auto">
  <name>Tarefa 2: Integrar enricher.py ao pipeline em geradores_core.py</name>
  <files>
    Sistema/motor/generators/geradores_core.py
  </files>

  <read_first>
    - Sistema/motor/generators/geradores_core.py (ler completo — especialmente as linhas 395-460 onde fica o fluxo da função gerar())
    - Sistema/motor/generators/enricher.py (recém-criado na Tarefa 1)
  </read_first>

  <action>
Fazer duas modificações cirúrgicas em geradores_core.py:

MODIFICAÇÃO 1 — Adicionar o import do enricher no bloco de imports do módulo
(junto com os outros imports de generators, após a linha `from generators._aliases import normalizar_dados`
ou no início do arquivo junto com os demais from generators.*):

Localizar a linha:
```python
from generators.cabecalho import build_header, add_page_number_footer
```

Adicionar IMEDIATAMENTE APÓS essa linha:
```python
from generators.enricher import enriquecer_dados
```

MODIFICAÇÃO 2 — Chamar enriquecer_dados() na função gerar(), APÓS normalizar_dados() e ANTES da validação de campos faltantes.

Localizar o bloco exato (aproximadamente linhas 399-401):
```python
    from generators._aliases import normalizar_dados
    dados = normalizar_dados(dados)

    # Campos que se faltarem, o documento fica "quebrado" ou ilegal (identificação)
    CRITICOS = {
```

Inserir uma linha após `dados = normalizar_dados(dados)`:
```python
    dados = normalizar_dados(dados)

    # Enriquecer dados ausentes com modelos narrativos do template (Fase 16)
    dados = enriquecer_dados(dados, template)

    # Campos que se faltarem, o documento fica "quebrado" ou ilegal (identificação)
    CRITICOS = {
```

IMPORTANTE: O import `from generators._aliases import normalizar_dados` na linha 399 é um import
local dentro da função gerar(). O import do enricher deve ser feito no TOPO do arquivo (nível de
módulo), junto com os outros imports de generators.*, NÃO dentro da função.

NÃO remover nenhuma lógica existente. NÃO alterar a validação de CRITICOS. NÃO alterar
o bloco de preenchimento de [PREENCHER]. O enricher simplesmente reduzirá a frequência
com que campos ficam faltantes, mas a lógica de fallback permanece como segurança.
  </action>

  <verify>
    <automated>
      cd "C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\Sistema\motor" && python -c "
import ast, sys

# Verifica que o import existe no módulo
with open('generators/geradores_core.py', encoding='utf-8') as f:
    conteudo = f.read()

assert 'from generators.enricher import enriquecer_dados' in conteudo, 'FALHA: import do enricher nao encontrado'
assert 'dados = enriquecer_dados(dados, template)' in conteudo, 'FALHA: chamada enriquecer_dados nao encontrada'

# Verifica que o módulo carrega sem erro
import generators.geradores_core as gc
print('Import OK')

# Verifica ordem: enriquecer_dados deve aparecer APÓS normalizar_dados no texto
pos_normalizar = conteudo.index('dados = normalizar_dados(dados)')
pos_enriquecer = conteudo.index('dados = enriquecer_dados(dados, template)')
assert pos_enriquecer > pos_normalizar, 'FALHA: enriquecer_dados deve vir APÓS normalizar_dados'

print('INTEGRAÇÃO VERIFICADA COM SUCESSO')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "from generators.enricher import enriquecer_dados" "Sistema/motor/generators/geradores_core.py"` retorna 1
    - `grep -c "dados = enriquecer_dados(dados, template)" "Sistema/motor/generators/geradores_core.py"` retorna 1
    - O import está no nível de módulo (não dentro de uma função) — verificável pelo grep mostrando que a linha NÃO tem indentação
    - O comando de verificação acima retorna "INTEGRAÇÃO VERIFICADA COM SUCESSO" sem erros
    - `python -c "import generators.geradores_core"` executado no diretório Sistema/motor retorna exit code 0
  </acceptance_criteria>

  <done>geradores_core.py modificado com import e chamada do enricher; pipeline carrega sem erros</done>
</task>

</tasks>

<threat_model>
## Limites de Confiança

| Limite | Descrição |
|--------|-----------|
| Template JSON → enricher | Modelos do template são internos; sem input de usuário externo |
| enricher → dados do documento | Output do enricher alimenta geração DOCX — strings malformadas podem corromper layout |

## Registro de Ameaças STRIDE

| ID | Categoria | Componente | Disposição | Plano de Mitigação |
|----|-----------|------------|------------|---------------------|
| T-16-01 | Tampering | _interpolar() | aceitar | Templates são arquivos internos do sistema, não editáveis pelo usuário final durante execução |
| T-16-02 | Denial of Service | re.sub() com regex | aceitar | Input é texto de template interno, sem risco de ReDoS por input malicioso externo |
| T-16-03 | Information Disclosure | dados dict | aceitar | Dados já transitam pelo pipeline sem proteção adicional; escopo local |
</threat_model>

<verification>
Smoke test end-to-end após conclusão das duas tarefas:

```
cd "C:\Users\diego\Meu Drive\02. Trabalho (Engenheiro Civil)\02. Prefeitura (PMO)\1. Gemini\02. Pareceres\Sistema\motor"
python -c "
import json, os
from generators.geradores_core import gerar

# JSON mínimo para habitese_comum (sem considerandos — forçar uso do enricher)
dados = {
    'tipo_relatorio': 'habitese_comum',
    'numero_processo': '3425/2026',
    'requerente': 'Sergio Esteves Vale',
    'logradouro': 'Rua Teste, 100',
    'bairro': 'Centro',
    'profissional_nome': 'Eng. Teste',
    'art_rrt': 'ART-001',
    'zona_uso': 'ZR1',
}
saida = 'Saida/_Testes/teste_enricher.docx'
os.makedirs(os.path.dirname(saida), exist_ok=True)
gerar(dados, saida)

# Verificar que o arquivo foi criado
assert os.path.exists(saida), 'DOCX não gerado'
print('SMOKE TEST PASSOU — arquivo gerado sem [PREENCHER] nos campos de texto')
"
```

Inspecionar o arquivo gerado e confirmar que não há texto [PREENCHER: considerandos] visível.
</verification>

<success_criteria>
- enricher.py existe com as funções enriquecer_dados, _interpolar, _tratar_condicional
- Todos os 9 testes unitários passam
- geradores_core.py importa e chama enriquecer_dados após normalizar_dados
- A geração de um habitese_comum com JSON mínimo completa sem erro
- O DOCX gerado não contém "[PREENCHER: considerandos]" quando o template tem modelo_considerandos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/16-refatoracao-gerador-docx/16-01-SUMMARY.md` com:
- Arquivos criados/modificados
- Resultado dos testes unitários
- Resultado do smoke test de geração
- Qualquer desvio do plano e justificativa
</output>
