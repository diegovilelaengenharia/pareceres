---
phase: 13-excelencia-operacional
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: [Sistema/motor/scripts/validador_fidelidade.py, Sistema/motor/tests/golden_dataset/*.json]
autonomous: true
requirements: [FR-04, NFR-01]
must_haves:
  truths:
    - "Existe um conjunto de dados 'Golden Dataset' representando a diversidade de processos da SMOSU"
    - "Script validador detecta divergências numéricas entre o JSON e o DOCX final"
    - "O motor processa os casos do Golden Dataset com 100% de precisão nos dados técnicos"
  artifacts:
    - path: "Sistema/motor/scripts/validador_fidelidade.py"
      provides: "Ferramenta de auditoria de dados"
    - path: "Sistema/motor/tests/golden_dataset/"
      provides: "Benchmark de qualidade"
  key_links:
    - from: "Sistema/motor/tests/golden_dataset/*.json"
      to: "Sistema/motor/generators/geradores_core.py"
      via: "Processamento via motor"
---

<objective>
Estabelecer um rigoroso controle de qualidade através de um Golden Dataset e automação de validação cruzada entre dados de entrada e documentos de saída.

Propósito: Eliminar o risco de erros de 'copiar e colar' ou erros de geração onde o JSON diz uma coisa e o DOCX diz outra.
Saída: Suite de testes de fidelidade e conjunto de benchmarks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@Sistema/motor/generators/geradores_core.py
@Sistema/motor/generators/componentes.py
</context>

<tasks>

<task type="auto">
  <name>Criar Golden Dataset</name>
  <files>Sistema/motor/tests/golden_dataset/alvara_ouro.json, Sistema/motor/tests/golden_dataset/habitese_ouro.json, Sistema/motor/tests/golden_dataset/pendencia_ouro.json</files>
  <action>
    Criar arquivos JSON de alta qualidade que sirvam como "padrão ouro" para os principais tipos de processos. 
    Esses arquivos devem conter dados complexos, incluindo:
    - Índices urbanísticos detalhados.
    - Histórico cronológico com pelo menos 3 eventos.
    - Partes envolvidas completas (Requerente, Proprietário, RT, Fiscal).
    - Memória de cálculo formatada.
  </action>
  <verify>
    <automated>ls Sistema/motor/tests/golden_dataset/*.json</automated>
  </verify>
  <done>Diretório de benchmark criado com casos reais simulados.</done>
</task>

<task type="auto">
  <name>Desenvolver Validador de Fidelidade (Cross-Check)</name>
  <files>Sistema/motor/scripts/validador_fidelidade.py</files>
  <action>
    Criar um script Python que:
    1. Carregue um JSON do Golden Dataset.
    2. Gere o DOCX correspondente usando o motor.
    3. Extraia o texto do DOCX gerado (usando python-docx).
    4. Compare campos numéricos críticos (Áreas, TO, TP, CA) e strings identificadoras (Processo, CPF/CNPJ, RT) entre o JSON e o texto extraído.
    5. Emita um relatório de 'MATCH' ou 'MISMATCH'.
  </action>
  <verify>
    <automated>python Sistema/motor/scripts/validador_fidelidade.py --test-mode</automated>
  </verify>
  <done>Ferramenta de validação cruzada operacional.</done>
</task>

<task type="auto">
  <name>Executar Auditoria de Qualidade</name>
  <files>Sistema/motor/tests/golden_dataset/relatorio_auditoria.md</files>
  <action>
    Rodar o validador de fidelidade para todo o Golden Dataset e documentar os resultados. 
    Caso haja falhas de extração ou formatação, ajustar os geradores em `geradores_core.py` ou `componentes.py`.
  </action>
  <verify>
    <automated>python Sistema/motor/scripts/validador_fidelidade.py --all</automated>
  </verify>
  <done>Auditoria concluída com 100% de fidelidade nos campos críticos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOCX -> Texto Extraído | O processo de extração para validação deve ser robusto para não gerar falsos negativos (ex: confundir '100.00' com '100,00'). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Spoofing | validador_fidelidade.py | accept | O validador é uma ferramenta interna de desenvolvimento, risco de spoofing é irrelevante. |
</threat_model>

<verification>
Verificar se o script `validador_fidelidade.py` falha corretamente ao injetar um valor errado propositalmente no DOCX.
</verification>

<success_criteria>
- Validador de fidelidade capaz de ler DOCX e comparar com JSON.
- Golden Dataset cobre os 3 principais casos de uso.
- Zero discrepâncias de dados entre entrada e saída nos testes finais.
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/13-excelencia-operacional/13-02-SUMMARY.md`
</output>
